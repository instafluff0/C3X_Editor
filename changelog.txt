* RELEASE 27.21.1
- Added a Units-tab FLC Workshop with legacy-compatible storyboard export/import, palette and FXM handling, Civ3 unit-FLC rebuild support, and civ-color-aware preview controls.
- Refined the FLC Workshop preview UI with a shorter modal, map-mode-style tabs, a frame-delay slider, and civilization thumbnail color selection based on each civ's default color.
- Fixed the FLC Workshop modal shell so its background stays opaque and its size remains stable when switching tabs.
- Tightened the FLC Workshop layout with a narrower preview modal, in-modal animation switching, and civ-color-aware palette previews with smaller swatches.
- Added simple FLC Workshop preview controls for indexed zoom and hour-based palette darkening, with Current Viewer Palette export using the same displayed palette.
- Organized FLC Workshop preview controls into display, playback, and transform groups, and let zoomed previews grow their display frame instead of clipping scaled-up units.
- Expanded FLC Workshop export options for current/custom/maximum frame size, current/custom/maximum frame count, original/viewer/selected palettes, and original/viewer/custom delay.
- Reworked the FLC Workshop Preview layout with compact controls stacked on the left and the animation plus facing pad centered in the main area.
- Reworked FLC Workshop export options into compact summary buttons with focused click-away editors for size, frame count, palette, and delay settings.
- Tightened FLC Workshop Preview controls so alpha/shadow, frame delay, zoom, and hour controls align as compact label-and-control rows.
- Simplified FLC Workshop export option editors with explicit close controls, no explanatory subtext, and custom inputs embedded in their matching choices.
- Matched FLC Workshop export summary font sizing to the smaller form inputs and removed the thin wrapper border around the Workshop tabs.
- Changed FLC Workshop to open and display a current FLC or storyboard source file, removed the separate Build tab, and made storyboard sources preview through the same tabs before saving them as FLC files from Export.

* RELEASE 27.21
- Added async QA warnings for unit animation entries that reference files other than `.flc`, while preserving vanilla `DEAD=.` placeholders.
- Added async QA warnings for unit `[Sound Effects]` entries that reference files other than `.amb` or `.wav`, matching vanilla unit sound-file conventions.
- Added approximate Units-tab AMB sound previews by parsing AMB KMAP WAV references, resolving component samples through scenario and vanilla unit-art roots, and playing them from the existing sound Preview controls.
- Fixed the Units list-panel header actions so Add All/Remove All buttons wrap inside their cards instead of overlapping neighboring panels.
- Fixed unit imports so direct runtime `.flc`, `.amb`, and `.wav` references in imported unit INI files can bring along sibling unit-folder dependencies, preventing imports like Gravity Armor from dropping reused Hover Tank sounds.
- Added async QA warnings for missing unit runtime INI, FLC, AMB, and WAV files, reporting the Civ3-style `Art\Units\...` path that would trigger FILE NOT FOUND.
- Fixed Scenario-mode Districts image previews so the era picker uses scenario BIQ era names when available, falls back to standard era buttons only when needed, and keeps detected art rows selectable.
- Added Map minimap dropup toggles for territory tinting, city dots, and resource markers with searchable per-resource filters, Check All/Uncheck All controls, map-icon thumbnails, and live resource counts while keeping the overlays separate from the cached terrain minimap.
- Added a Map editor Show Yields overlay that uses scenario-aware CityIcons.pcx crops to draw map-rule food, shield, and commerce icons from terrain, landmark, improvement, railroad, river, bonus-grassland, and resource yield data.
- Expanded the Map editor Display menu into grouped View Aids, Terrain, Map Features, Map Objects, and Scenario Markers controls, including separate toggles for tile improvements, resources, terrain features, borders, starting locations, and victory points, with sturdier panel clicks, Tile Info stacking, and chunk-cache invalidation so toggled layers stay consistent after scrolling or minimap jumps.

* RELEASE 27.20
- Fixed Map-tab unit overlays so map-only BIQ tabs resolve UNIT coordinates and PRTO icon/class metadata through the same support data used by Tile Info.
- Updated Map-tab city and colony deletes to immediately reindex surviving map references in the open editor state, matching the save cascade and avoiding stale tile pointers before save.
- Fixed BIQ map reference validation so map-only integrity checks do not misread absent civilization/player support sections as zero-length data, and kept civilization-delete blocker messages labeled as Scenario Players.
- Reduced large-scenario save overhead after opening the Map tab by omitting clean materialized map data from save/diff payloads and sending slim map payloads for ordinary map field edits and C3X sidecar-only map edits.
- Fixed Map tab imports and custom map creation so replacing a deferred/no-map tab immediately materializes the map state and keeps Open Map, Resize Map, and Remove Map available after the modal closes.
- Reused the just-loaded deferred scenario bundle for initial Quality Checks so scenario switching no longer asks the QA worker to reload the same BIQ immediately after the visible load.
- Fixed scenario QA resolution so text and art audits check all BIQ Scenario Search Folders in BIQ order before falling back to the BIQ directory.
- Deferred broad reference-art Quality Checks for scenarios with unopened large maps, running the rest of QA immediately and resuming the full art scan after the map materializes.
- Fixed Scenario Search Folders BIQ saves so edited semicolon-delimited search roots persist after Save and Reload.
- Reused the initial deferred BIQ parse when opening large maps, with file-staleness checks and fresh map-tab materialization so editor state is not shared with the cache.
- Fixed map resource rendering and resource-icon import handling for wide scenario `resources.pcx` sheets, matching width-based 50x50 atlas slicing so CCM3's pre-placed city resources display from multi-folder scenarios.
- Restored the Map tab Resize Map and Remove Map actions for deferred large maps, materializing map sections only when Resize is clicked.
- Fixed scenario C3X config loading so `scenario.*` config files are discovered across all BIQ Scenario Search Folders in order, while scenario saves still write to the primary local content root.
- Routed Fuji-sized world maps through the chunked map renderer at the default zoom, avoiding repeated whole-map canvas redraws during initial Map open.
- Fixed large BIQ scenario opens so deferred Map loads no longer send the full parsed BIQ, including every TILE record, to the renderer during initial load, and the Map tab no longer materializes the full map until Open Map is clicked.
- Fixed a Resources-tab blank-pane crash caused by Luxury city-icon preview type detection calling a config-only helper.
- Renamed the Resource import setting to cover all imported Resource art, including `resources.pcx` and Luxury city icons.
- Added scenario-aware Luxury resource city-icon import support for `luxuryicons_small.pcx`, including BIQ-order previews and non-fatal missing-art warnings.
- Added a Resources-tab type filter with Bonus, Luxury, and Strategic chips selected by default.
- Fixed Unit Table no-reload saves so successful saves clear stale row dirty badges in the open modal.
- Added async QA warnings for Civ3-long Civilopedia/PediaIcons art paths, with a staged repair action that shortens paths and keeps uploaded tech icons under the inferred 63-character runtime limit.
- Added the era-specific Science Advisor face and dialog art overlay to the Tech Tree screen, using scenario-aware `popupSCIENCE.pcx` and `dialogbox.pcx` assets.
- Added an Availability by Civ dropdown that lets the selected civilization immediately copy unit availability from another civilization in one undoable bulk edit.
- Fixed day/night async QA so scenario seasonal folders such as `Art/DayNight/Summer/2400` and `Art/Districts/Winter/1800` satisfy terrain and district art checks.
- Added developer-only SAV embedded BIQ extraction and save-inspection utilities, with tests isolated in a non-release debug tier, for debugging live-game rules, map resources, roads, units, and cities from saved games.
- Added a debug-only SAV inspector `--biq-out` option that writes a synthesized BIQ with embedded rules plus live terrain, resource, overlay, city, unit, and colony placement data.
- Fixed map natural wonder placement so `scenario.districts.txt` writes `district = Natural Wonder` with the selected natural wonder in `wonder_name`, matching C3X parser expectations.

* RELEASE 27.19
- Fixed Music async QA display so Music warnings show both as a tab badge and inside the Music tab content.
- Added Music async QA warnings for custom playlist MP3s outside the observed Civ3-safe 128 kbps, 44.1 kHz, stereo profile, which can freeze Civ3 when scenario music starts.
- Fixed newly added MP3 rows so drag/drop and picker imports inspect the source file immediately and show duration, bitrate, sample rate, and channel metadata before save/reload.
- Unified renderer and main-process debug logging so the in-app debug panel, stdout, and debug log file use the same formatted lines instead of rewrapping or duplicating renderer messages.
- Added Music now-playing waveform seeking before playback, displayed current time, total time, bitrate, sample rate, and channel mode in the sticky player, removed the redundant Selected/Playing status row, showed the play-button ring while seek metadata buffers, attached audio handlers before loading media, fixed cleared pending seeks being misread as seek-to-zero on Play, and added external music seek diagnostics for live reset/playback evidence.
- Reconciled the R27.19 Music and Custom Civ Colors branches so the final Music player/playlist UI and final civ palette editor ship together.
- Added a Civ -> Music editor organized by era and culture, with MP3 playback, drag/drop imports into scenario music folders, save/undo support, Files modal tracking, and import compatibility QA.
- Refined the Civ -> Music editor with path-aware play/pause state, a compact now-playing waveform seeker, and narrower era/culture playlist columns.
- Fixed scenario music inheritance so scenarios without local music show base-game music, then localize the playlist and MP3 files on scenario music saves.
- Documented Civ3 music layout, sparse scenario playlist behavior, and Music-tab UX implications in `docs/Civ3Music.md`.
- Stopped forcing ambiguous MP3 files into fake era/culture cells; they now remain playable in a matrix-integrated Playlist row.
- Fixed Natural Wonders animation saves so semicolon specs with comma-delimited seasons or offsets are not split into quoted fragments that C3X rejects.
- Added Civs tab color-picker usage hover details, Alternate color terminology, an outside-corner unused color marker, and reorganized the Colors/Diplomacy card into Colors and Diplomacy subsections.
- Added a Civs tab Assign Unique Colors action that gives each civilization a distinct color slot while preserving the first civilization already using each slot.
- Fixed Assign Unique Colors dirty tracking so every changed civilization gets a row badge and the Civs tab count reflects all changed rows.
- Fixed Custom Civ Colors table edits so changing row 7 by hex, RGB, or color picker uses the same linked-shade generation as Set Main Color, rebuilt from the stock Civ3 palette profile.
- Documented Custom Civ Colors storage, generation, scenario-local palette, and CIV-tab UI invariants for future editor work.
- Added a scenario-only Custom Civ Colors modal on the Civs tab, editing `ntp00`-`ntp31` palette files with a vertical palette editor, main-color generation for linked shades, per-color RGB/hex controls, scenario-local setup for shared BIQs, and save/undo support.
- Added compact batch Hue, Saturation, Balance, and Tint adjustments with live table and ramp previews for the active palette row filter.
- Changed the Custom Civ Colors row filter into compact combinable facets, including Main shades, Other useful colors, Gray, Protected, and All others.
- Matched the Custom Civ Colors row-filter styling to the Files modal checkbox-chip filters, with tighter spacing for the inline palette toolbar.
- Fixed unsaved Custom Civ Colors edits so Civs-tab color chips and color pickers repaint from the edited palette immediately, without requiring Save or tab reload.
- Fixed the Custom Civ Colors Main Color native picker so dragging inside the picker no longer closes it on the first live color update.
- Preserved Custom Civ Colors sidebar and row-table scroll positions when selecting another color set.
- Removed the redundant Custom Civ Colors Restore Color Set action.
- Added compact default-civilization usage summaries to the Custom Civ Colors sidebar rows.
- Renamed the Custom Civ Colors main picker label to Set Main Color and added hover/focus affordances around the label and swatch.
- Fixed Custom Civ Colors undo so restored palette data also refreshes the color swatches.
- Fixed Custom Civ Colors undo so the Set Main Color picker resets to the restored main color instead of keeping a stale draft.
- Fixed BIQ Government add/copy/import saves so every GOVT record's relation matrix is expanded to the final government count, and auto-repaired pre-existing GOVT matrix mismatches on later BIQ saves.
- Added subtle primary/secondary civilization color chips to the Civs list thumbnails.
- Added Terrain as the first Resources -> Generated By relationship group, editing TERR possible-resource masks from the selected Resource.
- Fixed Districts button tile previews to prefer scenario-local `Art\Districts\WorkerDistrictButtonsNorm.pcx` before falling back to the standard C3X sheet.

* RELEASE 27.18
- Fixed blank Technology placeholder staging so generated PCX conversion payloads survive template cleanup, and updated the Tides catalog parity baseline for the installed 241-improvement fixture.
- Added Scenario-mode tech PediaIcons coverage warnings and first-save tech icon block generation, including generated blank PCX placeholders when a Technology has no art.
- Fixed Resource imports so terrain rows that allowed the source resource now allow the imported resource too, preserving source scenario possible-resource terrain membership.
- Updated the Conquests unit-resolution baseline for the current Tides of Crimson scenario helper folders.
- Kept copied reference rows dirty when a pending copy is renamed back to a deleted original key before save.
- Added Standard Game as a Reference-tab Import source, so scenario BIQ entries can be imported from the global Conquests rules as well as other scenarios.
- Changed the Units `Unit Abilities` and `Available To` panels to show full checkbox lists, with ability labels and civilization thumbnails.
- Fixed the Units `Unit Abilities` checkbox labels so compact BIQ keys display as spaced friendly names.
- Fixed reference dropdown labels so copied/renamed Improvements and other reference entries show their current names without needing a full tab refresh.
- Fixed BIQ civilization copy saves so units available to the source civilization also become available to the copied civilization.
- Fixed BIQ civilization copy/delete saves so multiple same-save `@INDEX` operations keep targeting the original records instead of shifting later civ copies onto neighboring records.
- Added an Improvements `Required By Units` C3X reverse editor for `building_prereqs_for_units`, placed under Generated Resources.
- Fixed BIQ reference QA so Technologies with no era (`TECH.era = -1`) are treated as valid no-era techs instead of false missing-era warnings.
- Fixed no-reload BIQ save reconciliation so deleting a reference record updates already-open downstream reference fields, preventing shifted resources such as Helium-3 from temporarily displaying as the next resource until Refresh.
- Changed unit import so imported units start with no civilizations in Available To, and added a visible Add All action to the Unit Available To editor.
- Added async QA warnings for known BIQ reference fields that point outside their target tables, and made reference pickers display unresolved current values as invalid indexes instead of `(none)`.
- Fixed no-reload BIQ saves so successful saves with non-fatal warnings clear dirty badges and pending Save state, and kept same-session copied reference chains pointed at renamed pending source entries.
- Fixed Districts/Wonder Districts "Include in this scenario" saves so late progress events cannot reopen the save details modal after the save completes.
- Added app-wide Save and Undo keyboard shortcuts, with Ctrl+S/Ctrl+Z on Windows and Cmd+S/Cmd+Z on macOS routed through the shared main and modal action contract.
- Added lazy thumbnails to Terrain Type reference dropdowns, so Pollution Yields shows terrain art and Worker Terraform Action shows worker command-button art without preloading hidden option rows.
- Added a Map city Bulk Improvements modal with thumbnail multi-selects, city-owner/population/coastal filters, sorted affected-city preview with unchanged rows de-emphasized, clearer exact-set wording, and one-step undoable apply behavior.
- Tightened the single-city Map Improvements panel with compact rows, full-width search, aligned present-count text, and a Multi-City Edit action.
- Fixed single-city Map city edits so name, population, culture, owner, and improvement changes immediately enable the Map modal Save, Undo, and Undo All controls, including Cmd/Ctrl+Z for active improvement toggles.
- Fixed Units-list BIQ index hover details so they still work when visible index badges are hidden and are cancelled immediately when a unit drag starts or ends.
- Fixed the reference dirty-cache regression harness so it extracts the renderer dirty-state helper used by `rebuildReferenceDirtyCacheForTab`.
- Added a hostile multi-turn saved-BIQ unit regression that composes add, copy, delete, reorder, repeated save/reload, AI-strategy changes, PRTO self-reference lists, hidden strategy rows, and downstream unit-index references in one workflow.
- Added Units-list and BIQ save diagnostics for unit reordering, including drag start, highlighted drop target changes, row drops, pane-only drops, no-op drops, applied order fingerprints, PRTO primary remap summaries, hidden AI-strategy child-row remaps, and downstream unit-reference field families touched during save.
- Fixed Units-list BIQ index hover styling so rows keep the same thumbnail padding with indices shown and drag-enabled rows keep the grab cursor.
- Added a default-on File -> Settings -> Units toggle for Units-list BIQ index hover details and shortened the tooltip's first label to "Primary Index" to avoid truncation.
- Switched Units-list BIQ index annotations to the app's rich blue tooltip on row hover, including primary PRTO index and named hidden AI-strategy rows.
- Made Units-tab reorder Undo use a targeted in-memory snapshot instead of the slow full-tab restore/reload path.
- Added a default-on File -> Settings -> Units toggle for Units-tab drag reordering, with the list cursor and drag/drop affordances disabled immediately when the setting is off.
- Added app-wide dirty badge/count and Undo/Undo All contract tests covering editable tab families, reference rows, BIQ record lists, main toolbar controls, and modal undo/save button clusters.
- Fixed Units-tab reorder dirty tracking so moving a unit immediately shows the Units change badge/count and remains undoable via Undo All before save; reorder row badges now follow the units the user intentionally dragged instead of every row shifted by the final BIQ index plan.
- Added an optional File -> Settings -> Units toggle that shows subtle lower-right Units-list BIQ PRTO index annotations, including dynamically planned hidden AI-strategy row indices in parentheses.
- Added Units-tab drag reordering for scenario BIQs, saving PRTO primary order changes by BIQ index while remapping hidden AI-strategy child rows and downstream unit-index references.
- Improved Units-tab drag reordering with edge auto-scroll and drag previews that retain the unit thumbnail.
- Added exhaustive saved-BIQ unit reorder regressions across 2021 EXPANSION, Civilization LEGENDS, stock Conquests, and map/player fixtures, including full PRTO field snapshots, hidden AI-strategy rows, rule default units, map units, player start units, civilization king units, and building-produced unit references.

* RELEASE 27.17
- Fixed reverse-relationship source tooltips so BIQ structure records such as Worker Jobs report the loaded BIQ file instead of C3X effective config metadata.
- Added source/field tooltips to Tech Enables, Resource Required By/Generated By, and Government Required By relationship group labels and cards.
- Restored the searchable right-sidebar section list on Resources detail views.
- Added the Hide Sidebar / Show Sidebar toggle to Resources and Governments detail views.
- Aligned the Units prerequisites cards with the surrounding Unit Art and Unit Class sections by removing an extra horizontal inset.
- Removed the redundant inline Resources Civilopedia editor below Generated By now that Resources uses the top collapsible Civilopedia section.
- Reordered the Governments detail view so Civilopedia and Government Icons sit above Required Tech and Required By, matching the Resources layout.
- Added a Governments `Required By` board for Improvements and Districts that require the selected government, editing downstream Improvement Required Government and District Allowed Governments fields from the Government detail view.
- Added Resources `Required By` and `Generated By` boards below the Resource `Required Tech` controls, showing and editing downstream Unit, Improvement, Worker Job, District, and C3X generated-resource relationships from the selected resource.
- Fixed Refresh from Disk so the scenario selector reloads newly added, copied, or deleted `.biq` files before reloading the current bundle.
- Added async QA warnings and red wavy link highlighting for Civilopedia `$LINK` targets that point at missing Civ3 entries, catching imported scenario prose that still links to source-scenario Civs, units, concepts, or other absent entries.
- Fixed empty scenario Wonder District fallback screens so incompatible default wonder sets show the create/seed prompt instead of throwing a renderer error after scenarios filter them to zero entries.
- Added File -> Settings -> Tooltips presets for no delay, short delay at 300ms, medium delay at 800ms, long delay at 2s, or never showing shared blue metadata tooltips.
- Fixed reference picker metadata to target existing BIQ rows by `@INDEX` instead of Civilopedia key, preventing duplicate-key scenario data from saving as later same-key records.
- Fixed reference edit collection for existing BIQ rows with blank Civilopedia keys so edits still target the row by `@INDEX`.
- Fixed copied reference entries so their default copy name is written into the BIQ name field, preventing UI-created unit copies from reloading under the source unit's name.
- Fixed imported Unit and Improvement BIQ fields so they are saved as imported values even when the imported Civilopedia key already exists in the target BIQ, preventing target-template fields such as Improvement required tech from silently winning.
- Fixed blank Civilization creation so inherited city, military leader, and scientific leader list rows are not carried into new blank civs.
- Fixed imported Government relation rows so the pre-save imported editor state is expanded to the target BIQ government list before save/reload.
- Fixed blank unit creation so new PRTO rows use defaults shown after save/reload, including `otherstrategy = -1`, movement, AI Strategy, and order/action defaults.
- Fixed unit copy record operations to use BIQ index source references for duplicate Civilopedia-key units, so (for example) Civilization LEGENDS `War Chariot` copies clone the selected row instead of the first `PRTO_WAR_CHARIOT` row.
- Fixed concurrent BIQ record delete plus field-edit saves so edits targeting original indexes are shifted or dropped after deletes, and PRTO copy/delete now preserves or reconciles strategy-map child rows, preventing unit edits/adds/deletes from poisoning neighboring unit fields or AI Strategy masks.
- Added full-field saved-BIQ reload assertions for add, edit, copy, delete-survivor, and import CRUD workflows across Civilizations, Technologies, Resources, Improvements, Governments, and Units.
- Added semantic delete-cascade coverage for every inventoried BIQ reference field affected by CRUD deletes, including Rules default units, Civilization government/unit references, Unit and Improvement prerequisites, leader/player references, terrain resource masks, and government relation tables.
- Added save-after-each-prefix BIQ CRUD regressions for Rules default units, cross-entity Tech/Resource prerequisites, and Government references so fields shown in the UI are rechecked after each intermediate save/reload point.
- Fixed FLAV delete cascades so Civilization, Technology, and Improvement flavor bitmasks plus FLAV relation rows shift with surviving flavors, and broadened real-save pre-delete reference snapshots for structured BIQ sections such as Terrain, Worker Jobs, Eras, Difficulties, Espionage, and Flavors.
- Added saved-BIQ reload regressions for FLAV CRUD and Terrain/Worker Job reference cascades so flavor masks, terrain resource masks, worker jobs, worker-job tech requirements, and worker-job resource requirements are verified after intermediate saves.
- Added fixture-backed Civilization LEGENDS and 2021 EXPANSION unit edit/copy regressions so same-key PRTO primaries and strategy-map units are tested for adds as well as deletes.
- Added unit pending-reference regression coverage for logical PRTO primary indices when new units serialize with strategy-map duplicate records.
- Added full-field saved-BIQ reload assertions for newly added, copied, and imported PRTO units so regressions such as dropped unit names fail directly.
- Added BIQ regressions for newly added and imported units edited to multiple AI Strategy bits before first save, including checks that existing neighboring PRTO rows keep their fields and strategy masks.
- Added per-arrow floating Tech Tree border warnings for dirty Science Advisor arrows that touch protected advisor-frame areas, with red route highlights, stale route-snapshot invalidation for resized tech boxes, cleaner selected-focus route previews, a selected-arrow frame guide, no stray stage focus ring after handle deletion, and a save-details note.
- Improved Tech Tree drag responsiveness by caching selected-focus arrow layers and limiting border-warning refresh work during live tech-box and arrow-handle drags.
- Kept saved route metadata handles available on clean Tech Tree opens so precomputed Science Advisor arrows can be inspected and edited without first dirtying generated arrow art.
- Preserved clean-era Science Advisor arrow metadata when saving regenerated arrows for a different era, preventing an Industrial or Modern arrow save from dropping Ancient or Middle Ages hover handles.
- Added BIQ regression coverage for adjacent and duplicate-key unit AI Strategy masks, pending copied units, repeated unit saves, pending copied civilizations, repeated civilization saves, and civilization Flavor masks, guarding against save/reload transfers or losses in the areas reported by mod authors.

* RELEASE 27.16
- Collapsed trailing source-default C3X era-alias resets before positional backfill, preventing deleted middle-era aliases from pushing earlier blank eras to the previous custom name.
- Scoped C3X era-alias live autofill to the active focus session and committed serialized alias rows for Save/Undo state.
- Backfilled empty preceding C3X era-alias cells while editing and serializing, preventing later-era-only aliases from reloading into earlier eras.
- Renamed the Units contextual C3X card to C3X Rules and adjusted its checkbox spacing to match neighboring rule groups.
- Enforced BIQ header byte limits in the Scenario Title and Description editors, clipping by detected BIQ text encoding before save.
- Corrected Locked Alliances BIQ numbering, where slot 0 is No Alliances and slots 1-4 are Alliance 1-4, show alliance war states in a named matrix, and use a full-width layout without the field sidebar.
- Fixed GAME field enrichment so locked-alliance war flags keep their parsed BIQ values instead of reading the field-name suffix as the value.
- Removed the empty Player Options card from Scenario setup now that playable civilizations live in the dedicated Players panel.
- Patched Scenario and Rules BIQ field mappings for locked alliances, spaceship part requirements, culture level names, flavor relations, and read-only Civilopedia Entry fields to better match exposed fields.
- Added a read-only Total row to the scenario Time Scale table, corrected Base Time Unit to BIQ order, and made displayed ranges refresh when related time fields change without jumping scroll.
- Kept contextual C3X controls editable in Standard Game on Civs, Improvements, Units, and Techs while BIQ fields remain read-only, so changes save through the active C3X config target.
- Restyled C3X era alias matrices as bordered tables with transparent table-cell editing, while keeping the same structured alias editors and serialization.
- Added contextual C3X unit-rule toggles to the Units tab for PTW artillery targeting, sea-only bombarders, unit-per-tile-limit exemptions, and water defensive retreat limits.
- Audited C3X array-style base fields and tightened structured parsing so comma-delimited fields preserve newline-wrapped item bodies, while `show_tile_destruct_animation_after` now uses the token-list editor.
- Added contextual C3X era alias editors to the Civs tab so each civilization can edit its civ and leader aliases alongside its BIQ identity and leader fields.
- Replaced the C3X `civ_aliases_by_era` editor with a civilization picker and era table that groups noun, adjective, and formal-name aliases while preserving raw unmatched/duplicate source entries.
- Replaced the C3X `leader_aliases_by_era` editor with a leader picker and era table for replacement name, gender, and title aliases.
- Removed LF-only scenario text quality warnings and now silently normalize scenario Civilopedia/PediaIcons line endings to Windows CRLF on save, while skipping those text writes when the generated bytes already match disk.
- Drew selected-tech prerequisite and dependent arrow highlights on the top Tech Tree overlay layer, so temporary purple and green relationship arrows remain visible over unrelated tech boxes like selected blue arrows.
- Preserved explicit user-edited Tech Tree arrow routes during Science Advisor PCX saves, instead of re-constraining approved handles into generated-arrow safe bounds.
- Kept clean scenario Tech Tree opens on the saved Science Advisor PCX pixels even when arrow metadata exists, while still exposing saved route handles and temporary selected-arrow/relationship highlights for editing.
- Added async quality warnings for technology prerequisite loops, including self-prerequisite techs and circular chains that can crash Civ3 when opening the Tech Tree.
- Added a subtle colored glow behind selected and related Tech Tree boxes so highlighted prerequisite/dependent relationships are easier to read without relying only on outlines.
- Added a compact Background Arrows Delete handle button with the shared red x icon for deleting the selected non-endpoint Tech Tree arrow handle, with the same endpoint-protection rules as keyboard Delete.
- Fixed Tech Tree Auto-Position generated arrows so successful Tidy/Rebuild passes invalidate every generated route in the era, preventing stale route snapshots from leaving arrows detached after layout changes.
- Fixed Tech Tree arrow endpoint-handle drags so moving the source or target handle no longer rewrites the neighboring handle, preventing two-point arrows from disconnecting and preserving user-added middle handles.
- Refreshed an open Tech Tree modal after no-reload saves of generated Science Advisor arrow art, so saved arrows remain visible from the newly written PCX instead of disappearing when the dirty overlay is cleared.
- Opened the Tech Tree modal without preselecting the current Technologies row, keeping the initial title and box/arrow focus neutral until the user clicks inside the tree.
- Kept clean Tech Tree opens faithful to the base Science Advisor PCX by separating dirty generated-arrow preview from stored route-handle metadata, and cleared stale box/arrow selection when switching eras.
- Restored the previous Tech Tree arrow drag behavior while keeping the newer selected-arrow/selected-tech highlighting, route handle visibility, and explicit double-click/Delete route-handle editing for generated-arrow preview mode.
- Kept explicit Tech Tree arrow handle drags as raw user-edited routes instead of re-constraining them into generated right-angle paths after each move.
- Collapsed immediate out-and-back Tech Tree arrow route stubs before smoothing, preventing generated arrows from leaving small protruding handles in preview or saved PCX output.
- Fixed explicit Tech Tree arrow editing so double-click insertion survives selected-arrow rerenders and handle drags keep their pointer capture instead of losing the drag immediately.
- Added explicit Tech Tree arrow route editing: double-click an arrow segment to add a handle, click a handle to select it, Delete/Backspace removes selected internal handles, and Delete on a selected arrow clears its custom route override.
- Fixed Tech Tree arrow handles so a plain click selects the route without creating a route override, dirtying arrow art, or enabling Save/Undo; detailed arrow diagnostics now mirror into the daily app log.
- Added Tech Tree debug logs for generated-arrow hover, click, active-handle rendering, override creation, and handle drag start/end with edge keys and route points.
- Drew selected Tech Tree arrows and active route handles on top layers above tech boxes while leaving ordinary invisible arrow hit targets below boxes.
- Cached unchanged generated-arrow raster layers during Tech Tree box and arrow drags, so only moving route overlays are redrawn while boxes still move live with the pointer.
- Let Tech Tree arrow hit targets receive clicks through empty node-layer space while keeping actual tech boxes above arrows in the click order.
- Kept Tech Tree boxes above invisible generated-arrow hit targets, so clicking a box always selects/highlights the technology instead of accidentally selecting an overlaid arrow.
- Reduced Tech Tree drag-time work by rerouting only affected generated-arrow edges during box and handle drags, while keeping live box-drag arrows on fresh geometry instead of stale route snapshots.
- Kept generated Tech Tree arrows out of Science Advisor restored frame/border coordinates, including detours around the top-right notch before preview routes or saved PCX art can preserve them.
- Fixed Refresh from Disk while the Tech Tree modal is open so the modal rebuilds from freshly loaded scenario metadata and PCX previews instead of showing stale canvases.
- Fixed Tech Tree Auto-Position generated arrows so moved techs invalidate stale route snapshots and baseline hints instead of preserving old Science Advisor arrow stubs.
- Constrained Tech Tree Auto-Position to the same Science Advisor inner-frame geometry used when restoring saved PCX frame pixels, including the top-right notch exclusion.
- Added save-time Science Advisor arrow cleanup diagnostics that log residual arrow-colored pixel clusters and compare the current clear bounds against preview-style broad clearing.
- Changed generated Science Advisor PCX saves to restore fixed frame pixels before drawing new arrows, so edge-adjacent routes are not overwritten by the frame restore pass.
- Fixed generated Science Advisor PCX saves to use fresh Tech Tree preview route snapshots instead of recomputing stale dirty edges differently, and logged per-route save geometry for mismatch debugging.
- Improved Tech Tree background-arrow editing so selected arrows keep their handles after the cursor leaves, selected/hovered routes get prioritized hit targets, and selected technologies highlight their incoming and outgoing arrows.
- Fixed Tech Tree Auto-Position so dense Tidy layouts cannot keep overlapping boxes, using horizontal repair before accepting an existing-position layout.
- Changed Tech Tree selected-technology arrow highlighting to use a temporary contrasting color overlay, making related arrows easier to pick out in dense route tangles.
- Split Tech Tree selected-technology arrow highlights into exact-color incoming and outgoing overlays, and let plain background clicks clear the modal-only selected tech outline.
- Adjusted Tech Tree selected-technology arrow highlights so incoming arrows render purple and temporary arrowhead glints match their arrow body colors.
- Highlighted selected Tech Tree source tech boxes in purple and dependent target tech boxes in green to match the related arrow colors.
- Changed individually selected Tech Tree arrows to use a blue exact-color overlay so they stand apart from selected-tech source and target relationships.
- Fixed direct Tech Tree arrow clicks so selecting the edge itself immediately repaints the blue selected-arrow overlay, matching handle clicks.
- Shifted in-modal Tech Tree game boxes 4 pixels lower to better match the in-game Science Advisor, while keeping Auto-Position, arrow routing, drag saves, and stored BIQ coordinates on the correct display/storage coordinate split.
- Temporarily dimmed unrelated Tech Tree boxes and generated arrows while a tech or arrow is selected, keeping the focused relationship neighborhood visually prominent.
- Made Tech Tree selection dimming feel more immediate by applying focus classes before deferred arrow raster repaint and tinting the cached arrow layer instead of rerendering every unrelated route.
- Fixed clicking away from a selected Tech Tree arrow so unrelated boxes and arrows immediately undim instead of keeping stale arrow-focus dimming.

* RELEASE 27.15.1
- Kept era-less Tech Tree gutter technologies visually unconnected, so prerequisite arrows are only drawn between techs in the selected era.

* RELEASE 27.15
- Restored Industrial Science Advisor lookups to prefer `Art/Advisors/science_industrial_new.pcx`, matching common scenario art, while retaining `science_industrial.pcx` as fallback.
- Restored fixed Science Advisor frame/shadow pixels from the original PCX after generated-arrow cleanup, using the same inner polygon for every era and scenario.
- Persisted exact generated Tech Tree arrow route snapshots in the scenario-local sidecar so reopened Science Advisor previews match the saved PCX instead of recomputing slightly different routes.
- Added background quality warnings for dangerous scenario-local Civilopedia/PediaIcons shapes, including damaged `#HomelessIcons`, LF-only text files, suspiciously tiny overrides, and missing fallback EraSplash sections that can cause Firaxis editor freezes or in-game crashes.
- Documented the shared Save, Undo, and Undo All synchronization contract for the main UI and modal headers, including dirty badges, side-channel dirty state, and modal restore behavior.
- Fixed Tech Tree Auto-Position Undo so the main Save and Undo All buttons and Technologies dirty badges refresh after modal Undo returns the tree to its clean state.
- Fixed Tech Tree Auto-Position Undo so technology dirty badges are recomputed from restored X/Y data instead of leaving stale row badges behind.
- Fixed Tech Tree Auto-Position dirty badges so every moved technology row is marked based on changed X/Y coordinates, instead of only the selected tech.
- Added a Background Arrows `Snap drag` toggle so arrow route handles can either snap to nearby route axes or drag freely for more granular placement.
- Kept Tech Tree modal Undo and Undo All on the currently viewed era, so reverting moved tech boxes restores data and arrow mode without navigating to a different era.
- Kept untouched scenario Tech Tree eras on the original Science Advisor PCX arrows until the user edits that era, while still enabling generated-arrow handles immediately for eras loaded from the scenario arrow metadata sidecar.
- Removed faint stock Ancient Science Advisor pale scratch remnants by filling small holes inside cleared arrow masks and avoiding same-color replacement fills for those masked pixels.
- Restored broad generated Science Advisor arrow cleanup for stock and scenario backgrounds while keeping protected frame/chrome pixels out of the erase mask.
- Protected fixed Science Advisor frame/chrome regions from generated-arrow cleanup so stock and scenario backgrounds keep border texture while still removing old arrow remnants in the tech graph.
- Added a scenario-local editor-only Tech Tree arrow metadata sidecar so saved generated Science Advisor route adjustments are reused when reopening a scenario.
- Changed Tech Tree generated-arrow handles so previously saved scenario arrow metadata makes routes editable immediately when reopening that era.
- Improved generated Science Advisor arrow erasing near advisor-frame edges so cleared old arrows are filled from matching local background instead of smearing nearby frame texture into the parchment.
- Fixed generated Science Advisor arrow erasing so isolated fringe-colored background pixels are not cleared merely because they are near a real arrow.
- Grouped Tech Tree header controls into soft Scope, Display, Layout, and Background Arrows sections for easier scanning.
- Made the Tech Tree modal header more compact by removing redundant helper text and moving the live coordinate readout into the tree stage.
- Synced modal Save, Undo, and Undo All enablement so modal Save cannot be active when the matching modal Undo action is inactive, and Undo/Undo All now share the same undoable-state predicate.
- Added gentle nearby route-axis snapping while dragging Tech Tree arrow route handles, with Alt/Option-drag bypass for exact free placement.
- Fixed Tech Tree node double-click navigation so Back reopens the Tech Tree modal instead of staying on the Techs tab.
- Reduced Tech Tree flashing after dragging a tech by updating the moved node and generated-arrow preview in place instead of rebuilding the whole era.
- Added fuzzy Firaxis Science Advisor arrow-shape regression tests that compare generated route tangents and curve proximity against stock PCX arrow pixels.
- Fixed Tech Tree arrow route handle drags so each arrow-point move is its own Undo step instead of the next Undo reverting multiple previous arrow edits.
- Fixed Tech Tree single-step Undo after moving a tech so it restores the generated-arrow preview state as well as the tech coordinates, returning to the original PCX arrows when appropriate.
- Replaced the Tech Tree `Snap to Grid` checkbox with `Tidy` and `Rebuild` Auto-Position modes, with stock Conquests tests covering close-to-current cleanup and from-scratch Firaxis-like layout generation.
- Improved Tech Tree `Rebuild` Auto-Position so from-scratch layouts ignore current coordinate noise, keep stable prerequisite-branch ordering, and score visible generated-arrow crossings to avoid spaghetti routes.
- Constrained Tech Tree Auto-Position around the Science Advisor upper-right face area, using stock Conquests layouts to keep the reserved corner clear without shrinking the whole advisor frame.
- Tightened the Tech Tree Background Arrows style panel so color labels and swatches render as compact paired controls.
- Fixed Tech Tree `Auto-Position` bounds repair so close/tall stock-shaped layouts keep bottom-row tech boxes inside the Science Advisor frame instead of pushing them below the visible border.
- Improved Tech Tree arrow routing for close diagonal prerequisite boxes so routes prefer Firaxis-like bottom/top exits instead of odd side exits into nearby targets.
- Fixed Tech Tree `Auto-Position` so it scores box overlap as a hard failure, compares generated layouts against stock-shaped candidates automatically, compacts excessive dependency ranks into readable visual columns, and is regression-tested against Firaxis Conquests era layouts.
- Fixed generated Science Advisor PCX saves so arrow anchors use the same civ-scoped tech-box sizing shown in the Tech Tree modal, including unit availability filters and obsolete improvement icons.
- Added regression coverage that compares generated Science Advisor arrow preview pixels against the saved PCX output after palette quantization, including route overrides and custom arrow style.
- Reduced generated Science Advisor arrow erasure near game borders by requiring nearby true arrow-color pixels before clearing detached fringe-colored runs.
- Added a Tech Tree `Auto-Position` action that lays out the selected era left-to-right from prerequisites, aligns tech boxes on a grid, avoids overlaps, and favors generated-arrow routes that do not pass through unrelated boxes.
- Fixed Tech Tree modal Undo All enablement so it activates after the first unsaved change, including pending arrow-style edits before the global dirty flag refresh completes.
- Fixed Tech Tree node clicks so single-click selection and double-click close/focus are not misclassified as drag moves or arrow redraws.
- Changed generated Science Advisor arrow previews to clear the era background once and redraw all arrows with cached app-generated route baselines, while recomputing only edges connected to moved tech boxes.
- Added scenario-aware Science Advisor and `Art/Advisors/techboxes.pcx` rendering in the Tech Tree modal, including native background sizing, in-game techbox art, unit/improvement/worker-job unlock thumbnails for box size selection, and opt-in scenario-local Science Advisor arrow regeneration after moving techs.
- Refined generated Science Advisor arrows with shared preview/save raster output, coverage-based antialiasing, wider Firaxis-style arrowheads, and layered light glints on the arrow body.
- Added live Tech Tree controls for generated Science Advisor arrow colors, body thickness, edge softness, curve radius, and arrowhead geometry, with the same style passed to scenario PCX save output and color-picker redraws throttled for smoother dragging.
- Added Tech Tree modal Save and Refresh header actions that share the main app button handlers, labels, dirty state, validation blocking, and loading/saving disabled states.
- Added generated Science Advisor PCX arrow backgrounds to the Files modal as changed scenario write targets, with an Art PCX filter so queued arrow-art saves are visible before saving.
- Fixed Tech Tree Arrow Style dirty and undo handling so style-only edits enable Undo/Undo All, Reset is undoable only when it changes a custom style, and Undo All restores the clean saved style.
- Improved generated Science Advisor arrow cleanup for modded PCX backgrounds by removing connected and detached muted antialias/glint/shadow arrow pixels before drawing new arrows.
- Added session-only draggable Science Advisor arrow route handles in the Tech Tree modal, so regenerated arrows can be adjusted in preview and written through the normal scenario PCX save without creating extra route files.
- Improved generated Science Advisor arrow routing and editing by avoiding rounded reverse-turn kinks, preventing short-gap target approaches from bending back on themselves, preserving separated shared endpoint slots, removing avoidable mixed-side doglegs, entering side borders for vertically offset targets, choosing arrow-red palette colors instead of neutral near-matches, showing route handles only while hovering an arrow, and allowing arrow start/tip points to slide along tech-box borders.
- Matched generated Science Advisor arrows to era-specific Firaxis palette colors, including blue Modern Times arrows, cleared existing blue background arrows in previews and save output, limited scenario PCX regeneration to only changed eras, and added route-shape tests for Firaxis-like connector behavior.
- Added stock Conquests Science Advisor PCX regression coverage across Ancient Times, Middle Ages, Industrial Ages, and Modern Times for generated arrow endpoint sides and side-axis attachment placement, including the Monotheism, Theology, Chivalry, Feudalism, Invention, and Engineering cluster plus Industrial Combustion, Flight, Amphibious War, Advanced Flight, Mass Production, Motorized Transportation, Electronics, and Replaceable Parts routes, using Firaxis arrow pixels as the comparison target.
- Added read-only Worker Jobs command-button previews from Conquests `NormButtons.pcx` in Terrain -> Worker Jobs and Tech Tree unlock boxes, and documented that BIQ `TFRM` records do not store those atlas coordinates.
- Added obsoleted improvements to Tech Tree unlock boxes with Civ-style red X overlays, so they contribute to tech-box icon sizing and match the in-game Science Advisor.
- Added the in-game italic title and blue no-go marker for Tech Tree technologies flagged as not required for era advancement, keeping the marker as UI-only overlay art.

* RELEASE 27.14
- Renamed the focused BIQ map test file and npm test tier from map-critical to map for clearer maintenance naming.
- Changed the Map editor renderer to use bounded canvas chunks for oversized maps while keeping native scrolling, independent minimap rendering, and viewport-sized hover overlays, preventing large 362x306 maps from allocating multi-gigabyte full-world canvases.
- Smoothed large-map scrolling, minimap dragging, and Ctrl-pan by draining visible map chunks across frame budgets, retaining nearby chunk canvases, and prefetching adjacent chunks after interaction settles.
- Sped up large-map painting by keeping 1x1 brush resolution local, repainting only clipped edit regions inside already-rendered chunks, and patching changed minimap tiles instead of rebuilding the full minimap after every paint stroke.
- Fixed incremental minimap terrain-paint patches so edited tiles do not leave dark background rectangles or stale overlapping coastline artifacts on the minimap.
- Reconciled incremental minimap edit patches during idle time and widened their local repaint bounds, preventing subtle dark terrain-edge outlines without blocking paint gestures on a full minimap rebuild.
- Changed Map import to load source BIQs through a worker-backed map-only path and added a compact terrain preview to the import modal, avoiding full source scenario bundle/materialization work before confirming an import.
- Cleared `scenario.districts.txt` district and named-tile sidecar data when importing or otherwise replacing the whole scenario map, so coordinate-bound labels do not survive onto the new map.
- Added Deepwater Harbor map painting as a Sea terrain variant that converts Coast tiles to Sea while preserving their Coast graphics, with `DH` special-terrain markers on the map.
- Moved the Resize Map terrain-fill selector into the resize control grid above the minimap preview so it matches the Map Width and East/West control width.
- Fixed Map Resize undo so closing and reopening the Map editor cannot show the undone resized map from a stale Map tab button.
- Changed Scenario -> Players so the main search bar filters Playable Civs and removed the duplicate mini search inside the Playable Civs panel.
- Updated Tides catalog parity tests to follow the BIQ-declared scenario search root and the installed 242-improvement fixture baseline.
- Fixed Scenario -> Players search inputs so typing in either the player filter or Playable Civs filter keeps focus and caret position after the filtered list rerenders.
- Moved the Scenario Playable Civilizations checklist from Scenario -> Scenario into Scenario -> Players beside the player list, matching workflow while still saving the underlying GAME playable-civ fields.
- Cleared `scenario.districts.txt` district and named-tile sidecar data when a scenario map is removed, preventing coordinate-bound district data from surviving after BIQ map sections are deleted.
- Repaired scenario `PediaIcons.txt` saves so app-created files with real icon blocks inside `#HomelessIcons` are automatically normalized on the next scenario save, preserving Firaxis-safe homeless placeholders, CRLF line endings, and family-aware icon/wonder-splash block placement.
- Hardened scenario text saves so unrelated `PediaIcons.txt` comment/separator sections, EraSplash blocks, spaceship icon blocks, and Civilopedia spaceship articles survive reference edits unchanged.
- Fixed first-write scenario `Civilopedia.txt` and `PediaIcons.txt` saves to start from the loaded fallback text layer when the scenario-local file does not exist yet, preventing one-entry override files that drop stock references.
- Added icon-only Refresh buttons beside Save in the main toolbar and Map modal, reloading the current BIQ/config files from disk while preserving the active screen.
- Synced Map city wall art with the Improvements picker by updating the CITY Has Walls flag when the Walls improvement is selected or cleared, reusing the existing partial city redraw path.
- Preserved C3X District and Wonder District improvement-name references when an Improvement is renamed, including dependent improvements, wonder prerequisites, and conditional bonus entries, with async quality checks now warning on unknown conditional bonus improvement/terrain references.
- Aligned Scenario Players by exposing direct LEAD player Add/Delete controls and structured Starting Units editing in the Scenario -> Players panel, keeping playable civilizations separate from player-record count, and seeding added players with defaults.
- Matched Scenario Player save invariants by writing LEAD Any difficulty as `-2`, syncing WMAP civilization count to LEAD player count, and keeping SLOC/TILE starting-location state coherent.
- Normalized fixed playable-civ scenarios with preplaced city starts so player-slot-owned cities save as civ-owned stock-style cities and orphan TILE start flags are cleared when no SLOC records exist.
- Fixed Scenario Player saves with tile-only starting locations so harmless starting-location coherence notes no longer keep the Players tab dirty after a successful save.
- Added Scenario Players validation for playable civilizations that do not map to fixed LEAD player slots, warning in QC and blocking saves that can make Civ3 freeze while configuring AI players.
- Fixed Map city ownership reloads so Player 1-owned cities no longer display as ownerless, restoring borders, civ-color, and culture-specific city art after reopening scenarios.
- Added BIQ map regression coverage so barbarian owner zero and Player 1 owner zero cannot be conflated for city or unit records.
- Aligned Map owner pickers with ownership semantics by switching the owner list by owner type instead of unioning Player and Civilization choices.
- Matched direct-Barbarians map owner UI by hiding the Owner picker for Barbarian assignment and preserving map scroll when changing city or unit assignment.
- Removed RACE 0 Barbarians from Map Civilization assignment owner lists; Barbarian ownership is now selected only through the Barbarians assignment.
- Fixed civ-owned Map color rendering with custom player data so matching LEAD custom colors apply consistently to borders, city labels, unit overlays, and owner swatches.
- Made Map Tile Info Assignment controls full-width for cities, units, and starting locations.
- Refreshed open Map views when Scenario Player records or custom-player-data state change, keeping owner dropdowns, labels, colors, and culture art in sync.
- Refreshed open Map views when Civilization color or culture group fields change, so borders and culture-specific art update without closing and reopening the Map tab.
- Fixed Map rerenders after BIQ support edits so dirty Border Factor, live Civilization defaults, player color, and player era values are used immediately, with Border Factor now driving culture border expansion thresholds.
- Fixed Map modal startup on scenarios with Bridge districts by hoisting district-neighbor helpers used during the first canvas draw.
- Fixed Map district previews for configured art cells that are fully transparent, so placements still show the fallback district marker and log the transparent crop instead of appearing to fail.
- Fixed Improvement Gain reference dropdowns so `In every city` and `In every city on this continent` lazily populate Improvement option thumbnails like the other quick reference pickers.
- Cleaned up BIQ reference picker search placeholders so they use readable field labels instead of bunched raw keys.
- Removed the unused one-time Electron icon generator dependency and upgraded Electron to the patched 39.8 line, clearing the npm audit vulnerability report.

* RELEASE 27.13
- Fixed Scenario Civ art saves so generated Civilopedia icon sheets cannot overwrite the primary large icon when both slots start from the same source filename, with save/reload tests covering distinct PediaIcons paths and PCX dimensions.
- Fixed Improvement Required Government saves so a dirty Improvement tab resolves the selected Government through the full BIQ reference set and writes the BIQ instead of reporting no file changes.
- Added unobtrusive active-tab item counts for BIQ reference tabs, with Units counts ignoring synthetic era rows and strategy-map duplicate PRTO records.
- Documented the pending BIQ reference save flow, C3X/District name-reference boundary, and UI invariants for reference CRUD Undo, blank Add metadata, no-reload clean state, and dirty badges.
- Added save/reload regression coverage for pending BIQ entries referenced from C3X base and District configs, plus empty-index pending reference planning guards.
- Fixed blank Add templates so creating a new Civ, Tech, Resource, Improvement, Government, or Unit from an unsaved entry with staged art no longer inherits hidden pending thumbnail/import metadata.
- Fixed reference-tab Add/Copy/Import Undo so pending BIQ create operations are not left behind after the visible entry is restored, preventing undone Civ adds from still failing Save at the 32-civ cap.
- Fixed BIQ save planning for newly added Tech references so unsaved `biqIndex: null` entries cannot be mistaken for index 0, preserving Civilization free-tech assignments and similar pending references after reload.
- Added city-screen building icon atlas support for Improvements, including a vertical row picker from `Art/city screen/buildings-small.pcx`, selected small/large previews, combined scenario copy staging for both building PCX files, automatic imported Improvement row appends to `buildings-large.pcx` and `buildings-small.pcx`, and a Settings toggle for disabling those automatic imports.
- Added robust building city icon atlas tests covering Civ3 row geometry, display-mode column counts, BIQ/PediaIcons index allocation floors, palette-preserving appends, and deleted pending import compaction.
- Improved atlas picker responsiveness by caching decoded PCX preview canvases instead of rebuilding full-sheet RGBA canvases for every visible icon cell.
- Fixed no-reload saves after imported Improvement PediaIcons writes so quality checks recognize the just-saved scenario `#ICON_BLDG_*` block instead of showing a stale missing-block warning.
- Fixed imported Improvements with city-screen icon metadata but no loaded Civilopedia art paths so first save still writes the required scenario `#ICON_BLDG_*` block.
- Hid unreleased R28 C3X fields while the editor supports R27, including tile destruction animation settings now marked with R28 release metadata.
- Fixed blank Add templates for Civs, Techs, Resources, Improvements, Governments, and Units so new entries no longer inherit thumbnail/art paths or live reference defaults from the selected entry, and blank Civs now start with no favorite/shunned government, monarch unit, or diplomacy text slot.
- Fixed import/add modal field alignment so Name and Key inputs stay top-aligned even when the Key helper text is visible.
- Fixed the main Units tab so its sort menu no longer exposes the Unit Table-only `Manual` mode, stale saved `Manual` sort state falls back to in-game order, unsaved new Units stay surfaced before Save, and no-reload saves keep synthetic era-specific unit rows behind real BIQ unit records.
- Fixed unsaved Resource and Unit icon atlas pickers so every active pending import is drawn in its assigned future slot before Save, not just the currently selected import.
- Fixed no-reload saves after Resource/Unit atlas writes so pending atlas overlays are cleared and the next picker render re-reads the saved PCX instead of using stale previews.
- Removed the duplicate Units `Animation Folder Key` editor from `Unit Art and Animations` and moved the `#ANIMNAME_PRTO_*` PediaIcons indicator onto the remaining animation-folder control.
- Fix Windows icon to be properly sized and shown
- Fixed first-save BIQ reference planning so references to newly added Civs, Techs, Resources, Improvements, Governments, and Units resolve through final saved indices even after pending entries are deleted before Save, including unit lists, civilization bitmasks, PRTO strategy-map unit indices, and BIQ structure fields.
- Fixed automatic imported Resource and Unit icon atlas assignment so `resources.pcx` grid-only cells, BIQ-referenced high icon indexes, and deleted pending imports are handled consistently before Save.
- Added Worker Jobs to the Techs Enables board, editing TFRM required-technology references so techs like Steam Power can show and control worker jobs such as Railroad while reusing the Terrain worker-job thumbnails.
- Scoped the Great Wall auto-build wonder-name quality warning to active Great Wall auto-build configs, requiring districts, Great Wall districts, and auto-build to be enabled.
- Synced the Map modal Undo All state with tracked map dirty state so opening a scenario map without edits no longer enables Undo All from passive map display-field materialization.
- Made map resize shift or trim C3X scenario districts and named tiles alongside BIQ map entities.

* RELEASE 27.12
- Fixed Map saves so stale non-map BIQ field edits and resize-generated preview field edits are not collected during map-only saves, allowing successful terrain, overlay, and resize saves to clear the dirty Save state; also synced the Map modal Save button with the shared Save disabled/saving states, added volcano plus C3C overlay coverage.
- Upgraded the Units Table modal with an All/Civ availability filter, Manual column sorting with ascending/descending header indicators, and viewport-limited row/thumb hydration for faster large unit lists.
- Remembered the Show King Units preference separately for the Availability by Civ and Unit Table modals.
- Kept no-tech real units such as Warriors, Workers, and Settlers visible in Availability by Civ and Unit Table while still excluding PRTO era-variant pseudo units.
- Tightened new BIQ reference Civilopedia key editing so Add/Copy/Import keys follow the entered name casing from creation, preserve required prefixes like `TECH_`, show the 31-character limit, warn when spaces will become underscores, and auto-suffix duplicate keys before Save.
- Fixed reference and sectioned tab item selection so changing the selected Civ, Tech, Resource, District, or similar entry preserves the current detail scroll position instead of jumping back to the top.
- Added a Techs Unlocks board for editing the Units, Improvements, Resources, Governments, and obsolete Improvements tied to the selected tech through their real BIQ reference fields.
- Added Districts to the Techs Unlocks board, editing C3X `advance_prereqs` by tech name while preserving unrelated district prerequisites and precise Districts dirty counts.
- Added downstream Technologies to the Techs Unlocks board, editing other techs' prerequisite slots so a selected tech can show and control what it leads to.
- Improved Techs Unlocks rendering for techs with many unlocked entries by reusing each group's option list and deferring offscreen selected-entry dropdown mounting and thumbnail work until near the viewport.
- Changed Techs Unlocks undo capture to use scoped tab and section snapshots so Undo restores unlock edits in memory instead of triggering the full "Restoring previous change" reload modal.
- Changed Techs Unlocks add/remove/dropdown edits to refresh only the Unlocks board in place instead of rebuilding the whole Techs tab and resetting already-mounted list/detail art.
- Fixed Techs Unlocks edits so already-matching unlock references are not rewritten or marked dirty, and affected reference-tab dirty counts are rebuilt precisely after cross-tab edits.
- Added BIQ writer and no-reload clean-state guard tests so editable BIQ UI fields must emit save edits and successful no-reload saves must clear representative BIQ tab dirty state.
- Changed the derived BIQ Improvement category display field to read-only, leaving the persisted Wonder and Small Wonder bits as the editable category controls.
- Added automatic Scenario resource imports that append the source `resources.pcx` map icon into a scenario-local `Art\resources.pcx`, grow the atlas with a magenta row when needed, rewrite only the imported resource's icon index, and expose a default-on File -> Settings -> Resources toggle for disabling it.
- Added automatic Scenario unit imports that append the source `units_32.pcx` icon into a scenario-local `Art\Units\units_32.pcx`, grow the atlas with a magenta row when needed, rewrite only the imported unit's icon index, and expose a default-on File -> Settings -> Units toggle for disabling it.
- Added Map Resize edge-anchor controls for east/west and north/south growth or trimming, with matching minimap preview, save-time BIQ resize behavior, and fixture-backed coverage for placed map content staying on the intended tiles.
- Added a C3X base-tab `Next` warning control that jumps to the next warned setting row and marks warned rows inline.
- Added async C3X base quality checks for unmatched building, unit, resource, technology, and government names inside structured C3X settings such as generated resources, prereqs, perfume lists, and unit limits.
- Added a BIQ field inventory guard that fails when an editable or synthetic BIQ UI field is not represented in generated save/reload mutation coverage.
- Fixed Scenario Title and Description saves so those editor fields write to the BIQ header and persist after reload.
- Fixed no-reload Map saves so successful terrain, overlay, resize, and scenario district edits clear the Map dirty badge and Save button state instead of keeping stale BIQ/map originals dirty.
- Fixed map numeric parsing for BIQ reference display labels such as `CONT 1 (0)`, preferring the trailing game id instead of the first number in the label.
- Added fixture-backed BIQ coverage for 6 MP Age of Discovery fog visibility counts so loaded boolean TILE flags are checked through the map-modal interpretation path.
- Added fixture-backed BIQ save/reload coverage for 6 MP Age of Discovery map fields, including no-op edit collection and a real saved BIQ round-trip for Fog of War and city wall flags.
- Added broader stock-scenario BIQ serialization/deserialization coverage for projected reference labels, no-op map-section byte preservation, changed-section isolation, global reference integrity, and saved map edits across WMAP, TILE, CITY, UNIT, CLNY, and SLOC records.
- Fixed reference-tab dirty badge counts so newly added/copied entries that have their keys edited are counted once instead of retaining stale pre-rename dirty identities.
- Moved Civs Grammar / Culture directly after Identity in the two-column layout and corrected the section heading spelling.
- Fixed Civs Colors / Diplomacy rows so the Diplomacy Text input shrinks inside the card in two-column layout instead of overflowing into the neighboring panel.
- Fixed no-reload saves so newly added or copied BIQ reference entries receive their saved BIQ indexes and move into reload-equivalent index order immediately after Save.
- Fixed copied BIQ reference entries so fields inherited from an unsaved edited source are saved onto the copied record too, preserving chained new Tech prerequisites and similar references after reload.
- Fixed BIQ saves so reference/structure record operations and field edits force BIQ save planning even if cached renderer dirty-tab counts only report non-BIQ config changes, preventing add/import/edit/delete changes such as removed Improvements from reappearing after restart.
- Fixed reference-entry deletes so quality-check warnings attached to the deleted item are removed and later warning indices shift with the surviving entries, preventing an Improvement warning from appearing on the next Improvement after delete.
- Added inline Create/Copy Scenario modal validation that disables the action button and shows a destination-folder warning when the target scenario/search folder already exists.
- Added File -> Settings -> Reload After Save as a persisted, default-off option; when disabled, successful saves now keep the current bundle open, clear one-shot save metadata, and mark the in-memory data clean instead of reloading the app state, while leaving BIQ changes visibly dirty if the BIQ save reports skipped edits or warnings.
- Fixed no-reload saves so quality checks rerun against the current in-memory bundle after Save, clearing stale warning panels and badges without restoring the full post-save reload.
- Fixed repeated Map terrain painting after Undo by restoring diff-undo TILE records in place, keeping the open map editor's coordinate lookup aligned with the restored terrain before recalculating transition file/image values.
- Fixed history Back from Map editor reference links so returning from linked entries, such as city building improvements, reopens the Map editor instead of landing on the plain Map tab or another reference view.
- Skipped the full post-save bundle reload for successful saves limited to C3X base and sectioned config files, marking the current in-memory bundle clean instead while keeping the existing reload path for BIQ, scenario search-folder, text/reference/art, Unit INI, and scenario district sidecar saves.
- Skipped scenario BIQ edit collection during Save when the dirty tab set is limited to non-BIQ config files, while retaining the existing BIQ path guard and automatically re-enabling collection whenever BIQ-affecting tabs or auto-created scenario search folders are involved.
- Changed scenario BIQ Save planning to pass the edited BIQ buffer directly into the existing atomic save transaction, avoiding an extra temporary BIQ write/read cycle while preserving rollback behavior and in-place BIQ rewrites used by scenario creation.
- Skipped the full save-time bundle reload used for unsafe BIQ reference-delete validation when a save has no reference delete operations, preserving the existing validation for deletes while reducing ordinary scenario Save work.
- Fixed Map modal city creation so new cities no longer inherit cloned building lists, walls, or palace flags from the first existing city record.
- Fixed the Files modal to track pending `scenario.districts.txt` writes from Map modal district and named-tile edits, including View Changes diffs before save.
- Fixed Map modal reopen after Select-mode district edits by resyncing scenario district sidecar entries back onto live TILE display fields before each map render.
- Changed Map city name and population edits to use clipped map redraws instead of full canvas refreshes, while leaving territory-affecting city edits on the existing full refresh path.
- Reused already-parsed BIQ map record fields when materializing the deferred Map tab, avoiding duplicate English-field parsing while preserving the existing BIQ load and save pipeline.
- Fixed BIQ-backed World/Terrain/Rules/Scenario field Undo to restore scoped tab snapshots instead of taking the slow scenario reload path for ordinary edits.
- Deferred reference-picker dropdown row construction until the picker opens, reducing Rules -> General startup work from hidden unit-reference menus.
- Fixed reference-tab `Next warning` jumps so the left item list scrolls the newly selected warned entry into view.
- Changed Districts, Wonder Districts, and Natural Wonders selection to paint the active list-row state before deferring the heavier detail-pane rebuild, matching the BIQ reference-tab responsiveness path.
- Changed BIQ reference-tab selection to paint the active list-row state before deferring the heavier detail-pane rebuild, so Units and similar tabs respond to clicks before paying detail render cost.
- Fixed Game Concepts tab warning badges so Civilopedia link warnings counted on individual entries also appear in the top tab name.
- Changed Districts, Wonder Districts, and Natural Wonders structural actions and atlas-copy staging to use section-scoped Undo snapshots, and grouped Natural Wonder animation text edits into normal edit-session Undo steps.
- Tightened C3X/base structured-list Undo capture so add/remove actions keep one scoped base snapshot per click and grouped base inputs explicitly use their field-level Undo key.
- Fixed the Availability by Civ header Save button to sync its disabled state immediately on modal open, matching the main app Save button when there are no unsaved changes.
- Added a duplicate Save button to the Availability by Civ modal header, sharing the main Save button's label, dirty styling, disabled state, validation title, and save handler.
- Moved Availability by Civ Undo and Undo All into the modal header beside Close, matching the Unit Table and Tech Tree modal action layout.
- Changed Governments relation-table edits, Improvements custom compact/top/art controls, and Resources map-icon edits to use entry-scoped Undo snapshots instead of full-tab or full-bundle restore snapshots.
- Changed Techs top-board edits and Civs starting-tech, custom name-list, build-priority, and diplomacy editors to use entry-scoped Undo snapshots instead of full-tab or full-bundle restore snapshots.
- Changed the Availability by Civ modal to keep its unit rows mounted, debounce search filtering, cache per-civ availability during refreshes, and lazy-hydrate visible thumbnails, so civ switches and availability toggles no longer rebuild the whole grid on every interaction.
- Fixed Availability by Civ search row filtering to hide non-matching mounted cards instead of only updating the shown-count summary, so searches now visibly narrow the grid again after the mounted-row optimization.
- Changed Availability by Civ Undo and Undo All to refresh the mounted modal in place against the live Units tab instead of reopening the whole modal after each undo, so undoing availability edits no longer tears down and rebuilds the dialog DOM.
- Fixed shared reference-tab thumbnail hydration after selection changes by rebuilding the pending thumbnail queue from preserved list rows, so scrolling Units, Improvements, and similar tabs after selecting a new item still loads thumbnails as rows enter the viewport.
- Fixed Districts, Wonder Districts, and Natural Wonders thumbnail hydration after selection changes by rebuilding the pending section thumbnail queue from preserved list rows, so scrolling after selecting an entry still loads newly visible art.
- Changed Units and other reference-tab Civilopedia edits to capture entry-scoped Undo snapshots, so undoing a completed Civilopedia text edit no longer needs the full scenario restore path.
- Changed reference art slot edits, including Unit Civilopedia icon replacement and pending filename renames, to use entry-scoped Undo snapshots instead of the full restore path.
- Changed reference-tab top `Name` edits to use the same entry-scoped Undo snapshot as Units rule fields, so undoing a unit rename restores in memory without showing the full restore modal.
- Fixed the Unit Table `Undo` button state to activate on the first pending grouped edit instead of waiting for a later committed snapshot, so the modal header now reflects the first typed change immediately.
- Added a duplicate Save button to the Unit Table header beside Undo, sharing the main Save button's label, dirty styling, disabled state, validation title, and save handler.
- Changed Unit Table reference columns to reuse one cached option list per column and lazy-mount real dropdown pickers only when a cell is activated, so opening the modal no longer instantiates full picker menus for every reference cell up front.
- Fixed Civs -> Leader Animations era labels to use the loaded BIQ ERAS names, matching Techs era dropdown behavior instead of hardcoding the stock era names.
- Changed Unit Table numeric cells to use constrained integer inputs and sanitize edits before writing to the Units model, matching the main editor's numeric-field behavior more closely.
- Added a continuous outline treatment to the focused Unit Table row, including sticky thumbnail/name columns, so the active edit target has a visible whole-row border without drawing dividers between cells.
- Cached repeated BIQ section-index and enum option lookups within a single `RULE`/`GAME` render, so tabs like Rules -> General no longer recompute the same PRTO/RACE/TECH option lists for every reference field while opening.
- Changed BIQ `RULE`/`GAME` record-group rendering to mount heavy group cards incrementally across animation frames, so opening one-record tabs like Rules -> General paints the shell immediately instead of blocking on every field editor at once.
- Changed the sectioned Districts/Wonders/Natural Wonders shell to keep its left list mounted across selection changes and use the newer viewport-prioritized thumbnail hydration queue, so clicking between long district-style entries no longer rebuilds every list row and fast scrolling is less likely to leave section art blank.
- Unified Civs, Resources, and Governments with the shared deferred reference-tab utility-section renderer, so their Civilopedia and art panels now lazy-mount like Units, Techs, and Improvements instead of eagerly building closed editors.
- Fixed Improvements -> Required Districts dropdown thumbnails by wiring that picker through the shared district preview renderer, so district options now show their representative art instead of empty placeholder squares.
- Fixed shared reference-list thumbnail hydration to retry a couple of likely transient blank visible slots instead of permanently giving up after one missed load attempt, so fast scrolling is less likely to leave icons unrendered.
- Changed shared reference-tab utility sections such as Civilopedia and Tech/Improvement art panels to lazy-mount their heavy editors only when opened, so long tabs reuse the same fast shell without paying closed-section render costs up front.
- Changed Units reference-list thumbnail loading to use a small viewport-prioritized in-flight queue, so fast scrolling no longer gets stuck behind thumbnail requests for rows you already flung past.
- Fixed reference-list lazy thumbnail hydration to keep scheduling follow-up viewport passes while visible rows are still pending, so Units list icons now appear as items scroll into view instead of waiting for a click or extra scroll nudge.
- Stopped hidden reference-picker dropdowns from preloading option thumbnails while a tab detail pane is being rebuilt, so selecting a new Unit no longer fans out into thumbnail loads for unopened prerequisite/resource/upgrade pickers.
- Kept automatic reference-art quality checks enabled, but deduplicated repeated art path resolution and in-flight preview requests so startup on tabs like Units no longer stampedes the same icons and `units_32.pcx` fetches while still reporting missing art warnings.
- Changed reference-tab selection and filter rendering to keep the existing Units list DOM alive where possible, so selecting a different unit now refreshes the detail pane without rebuilding every list thumbnail and search can keep the current detail pane when the selected entry still matches.
- Fixed a Map modal open-time crash in the new Port renderer by hoisting the shared `wrappedDelta` helper so the initial canvas redraw can compute port-facing city deltas before later helper declarations run.
- Fixed Map modal Undo after district, natural wonder, and overlay edits by routing those edit categories back through full map snapshots instead of the record-diff shortcut, since those actions can mutate scenario district sidecar state or helper-managed overlay structures that the current diff restore path does not fully reconstruct.
- Changed Map modal Port district rendering to follow C3X `align_variant_and_pixel_offsets_with_coastline` for directional variant selection, so ports now choose the same coastline-facing sprite from nearby friendly-city and shoreline context without yet applying the in-game pixel-offset adjustments.
- Changed Map modal Bridge district rendering to follow C3X `get_bridge_image_index`, so bridges now choose the same directional/rail variants based on neighboring bridge tiles and adjacent land links instead of using generic district building-column selection.
- Fixed Map modal Great Wall district connections for neutral and otherwise non-territory-owned tiles by matching C3X's raw `Territory_OwnerID` comparison, so adjacent walls now link when their owner ids match even if neither tile has an explicit territory owner.
- Changed Map modal Great Wall district rendering to follow C3X's base-plus-segment wall logic, so Great Wall tiles now layer their pillar and same-owner neighboring wall segments from `GreatWall.pcx` instead of using the generic district-cell preview crop path.
- Changed Map modal canal district rendering to follow C3X's neighbor-aware directional canal logic, so canal overlays now choose and extend the same directional sprites the in-game renderer would use instead of relying on the generic district-cell preview crop path.
- Fixed Civs `Favorite Govt` and `Shunned Govt` picker dropdown thumbnails so the BIQ field editor actually enables government option thumbnails in the menu, matching the already-thumbnail-enabled selected-value chip path.
- Changed C3X/base Undo to use scoped base snapshots and in-memory restore, so base-field undos no longer reload the whole scenario before recomputing the base tab dirty state.
- Further aligned Unit Table editing with the main Units tab by making its text and number cells use the same grouped entry-scoped undo sessions and shared Units dirty-key set, reducing modal-specific undo and dirty-state divergence.
- Unified Unit Table text and number cell undo sessions with the main Units editor by routing them through grouped entry-scoped undo keys, and changed Unit Table dirty badges to read the shared Units dirty set instead of maintaining a separate modal-only dirty source of truth.
- Reduced Unit Table Undo stalls by stopping the modal from rescanning every unit on each dirty refresh; it now builds the Units dirty cache once when the modal opens and keeps it incremental for live edits and row-scoped undos.
- Changed generic section text and number editors in C3X, Districts, Wonder Districts, Natural Wonders, and Animations to use one grouped undo step per focus session instead of pushing a new section snapshot on every keystroke.
- Added section-scoped undo snapshots and fast section-restore dirty updates for sectioned editors such as Districts, Wonder Districts, and Natural Wonders so ordinary single-section undos no longer have to restore and recompute entire section tabs.
- Moved Tech Tree modal `Undo` and `Undo All` controls into the modal header beside `Close`, matching the Unit Table modal layout.
- Fixed a renderer regression that left the Improvements tab blank by restoring the missing `tabKey` and selected-entry context passed into the Improvements top-board builder, so its undo-key wiring no longer throws while rendering.
- Fixed Map modal hill painting to use a grassland base beneath hills, matching the existing mountain/volcano handling and other editors' map semantics so coastal and edge hill placements no longer leave the wrong underlying terrain.
- Reverted clipped local-redraw handling for Map modal Undo after it produced terrain and overlay corruption; diff-based Undo still uses the faster map-specific restore path, but now falls back to the safe full map refresh.
- Fixed the new diff-based Map modal Undo path to write the restored map tab back before refreshing visuals, and updated the map-redraw regression coverage so targeted Undo repaints can be validated without falling back to the old full-refresh test assumptions.
- Routed tile-only Map modal Undo operations through the existing clipped local-redraw path, so diff-based Undo can refresh just the affected map region and defer minimap work instead of forcing a full 8,160-tile repaint.
- Cut Map modal Undo apply time by routing map-only restore through a map-specific UI refresh path, avoiding the generic full tab-strip and active-tab rerenders while still rebuilding the open Map modal from the restored map state.
- Switched district, natural wonder, overlay, and visibility map edits onto the existing record-diff undo path and added explicit Map modal undo timing logs so Undo no longer depends on full serialized map snapshots for those edit types and any remaining delay can be profiled directly.
- Added Unit Table dirty badges beside unit thumbnails and a focus-driven row highlight so changed units and the actively edited row are easier to track during bulk edits.
- Stopped normal rules-editor dirty refreshes from forcing expensive map dirty checks just to update hidden Map modal undo buttons; those button states now refresh only while the Map modal is actually open.
- Expanded the `AGENTS.md` daily log note to include both macOS and Windows log paths so future debugging guidance matches the user's current machine.
- Documented the daily app log location in `AGENTS.md` so future agents can tail the current-day log directly during debugging instead of asking users to paste log output.
- Fixed clipped Map modal edit redraws so long river and overlay pixels are repainted from farther source tiles when a nearby tile edit clears their area, preventing partial river cutoffs a few tiles away from district edits.
- Added focused `biq-ref` performance logs for reference-edit undo capture, dirty comparison, and dirty-refresh work so normal rules-editor lag can be profiled alongside the existing map instrumentation.
- Added a second-pass `biq-ref` refresh breakdown log that times each `refreshDirtyUi()` sub-step and records why each scheduled dirty refresh ran, so UI-side stalls can be separated from data and undo work.
- Added grouped Map modal terrain-sprite miss logging so fallback-color terrain redraws can be traced to missing atlases, invalid image indexes, or related atlas lookup failures from a single debug log.
- Fixed a Map modal regression where hover-only district preview rerenders could suppress the pending full terrain-art refresh, leaving the map stuck on flat fallback colors and glyphs even after terrain atlases finished loading.
- Stopped district hover-preview art cache misses from forcing full Map modal canvas and minimap rerenders; newly loaded district preview variants now refresh the hover layer in place.
- Reduced Rules editor input lag by scoping many unit/civilization/technology/improvement undo snapshots to the active entry, caching clean reference dirty signatures, and stopping several non-map editors from accidentally snapshotting the whole map tab on each change.
- Reverted an overly aggressive Map modal partial-redraw candidate optimization that could clear large edit regions without repainting every affected tile, leaving visible blank map holes around edited terrain.
- Fixed Map modal single-step Undo for diff-based map edits so map-only undo snapshots patch the live map tab instead of falling through the generic empty-snapshot tab reset path, which left Undo appearing to do nothing.
- Removed the remaining whole-map dirty comparisons from the hot Map modal mouse-up path when map edits are already known dirty, so Save-button refresh and hover/selection overlay updates no longer stall on another full map diff after painting.
- Cleared the Map modal hover/paint preview overlay immediately when a paint stroke ends so stale brush outlines do not linger after the edited terrain has already redrawn.
- Switched hot Map modal paint and selected-tile edits to a fast known-dirty path so Save-button enablement and hover/selection overlay cleanup no longer wait on a whole-bundle dirty snapshot the first time the map becomes unsaved.
- Kept the Map modal hover/brush preview hidden after paint mouse-up until the pointer actually moves again, preventing post-stroke hover redraws from making the selection/preview overlay appear stuck.
- Added map hover-layer clear and dirty-UI refresh timing logs so lingering post-paint overlays can be compared directly against Save-button enablement and other deferred map UI work.
- Added deeper Map modal timing logs for tile hit-testing, brush resolution, undo snapshot capture, terrain edit application, redraw, minimap refresh, and tile-info rendering so slow map edits can be profiled from one debug-log trace.
- Reduced Map modal edit latency by skipping redundant re-stringification for serialized map undo snapshots and deferring map undo dirty-UI refresh out of the immediate click path.
- Improved Map modal paint responsiveness by deferring minimap refresh until after the main map redraw and added explicit `...-settled` timing logs for edit completion.
- Reduced Map modal terrain/resource/fog edit latency further by using record-diff undo entries on the hot paint/select paths instead of full-map undo serialization.
- Fixed Units `Unit Table` reference-jump buttons such as `Upgrade To` so they close the modal before navigating to the selected unit instead of leaving the modal overlay in place.
- Fixed Units `Unit Table` history so jumping from the modal to a unit detail view lets `Back` reopen the Unit Table with its prior filters.

* RELEASE 27.11
- Fixed the Map modal `Save` button state so it follows the main app rules and stays disabled unless there are unsaved changes and save is currently valid.
- Fixed BIQ auto-encoding detection to read BIQ/map strings from the BIQ itself instead of inferring from Civilopedia/PediaIcons text layers, which corrects mixed-language cases such as accented city names rendering as the wrong characters on the Map tab.
- Deferred full Map-tab BIQ projection until the Map tab is actually opened, improving app load time and scenario switching for large BIQs while preserving on-demand map editing and import behavior.
- Enabled the new Map tab with in-app scenario map importing, resizing, removal, and editing.
- Renamed the File menu toggles to `Enable Custom Rules` and `Enable Custom Player Data`, and kept them visible but disabled outside Scenario mode instead of hiding them entirely in Standard Game mode.
- Unified `Scenario -> Players` custom-player-data field labeling so newly enabled previews and existing BIQs both use the same human-readable `LEAD` labels, and fixed numeric start-unit keys like `starting_units_of_type_0` to render as resolved unit names such as `Settler Units` instead of raw `0 Units`.
- Fixed scenario custom-rules and custom-player-data previews to source civ/government/era and similar BIQ picker options from the live in-memory tab sections instead of the stale pre-toggle BIQ section list, restoring automatic default population for tabs like `Scenario -> Players` before save.
- Fixed `Custom Rules` preview state so the cloned `Terrain`, `World`, and `Rules` BIQ tabs no longer show unsaved-change badges just from the toggle itself; they now stay clean until edited against the preview baseline.
- Fixed `Rules -> General -> Default Units` and `Default Money Resource` BIQ fields to use the structured reference pickers in scenario custom-rules previews instead of falling back to plain raw-value controls.
- Made unit `PediaIcons` mappings explicit in `Unit Art and Animations`, with direct editable fields for the `#ICON_PRTO_*` large/small Civilopedia paths and the `#ANIMNAME_PRTO_*` animation folder key.
- Added a new Units `Unit Table` modal for batch editing existing scenario units in-app, with thumbnails, sticky name columns, local modal Undo/Undo All, Apply-once save behavior, and arrow-key spreadsheet-style navigation across core unit stats and prerequisite fields.
- Moved scenario `Custom Rules` and `Custom Player Data` toggles from the header into the OS `File` menu, aligned custom-player-data enablement with default eight-player LEAD setup, and changed post-load quality checks to audit the live in-memory BIQ preview so toggle-driven fallback/disable states stop producing stale warnings.
- Fixed scenario BIQ loads with `Enabled Custom Rules` off to fall back to standard-game rule/reference tabs instead of synthesizing broken scenario-local resources/civs/techs/units, which also restores correct map resource rendering and disables those tabs in the UI.
- Split the test suite into fast, BIQ, and full npm tiers so day-to-day verification can avoid the longest BIQ round-trip and CRUD suites while keeping them available for release checks.

* RELEASE 27.10
- Added direction badges to Wonder District art previews when alternate-direction art is enabled, marking main art as right-facing and alt art as left-facing.
- Fixed the Units tab responsive layout so the right sidebar no longer overlaps the dense rules content before the content grid collapses.
- Added an Improvements `Required Districts` C3X convenience field that edits matching Districts `dependent_improvs` entries and supports multiple required districts.
- Added inline Wonder District art row/column controls with a larger, right-aligned sheet-cell selector for completed, construction, and enabled alternate-direction art coordinates.
- Fixed BIQ-backed Small Wonders so Wonder District name search uses the BIQ category flags instead of requiring matching Civilopedia/PediaIcons wonder metadata.
- Removed the placeholder `New Wonder` entry from Wonder District name pickers.
- Fixed Wonder District cell pickers so changing the image path or custom cell size reloads the picker from the current PCX.
- Added a Districts Button Tile sheet picker for selecting `WorkerDistrictButtonsNorm.pcx` row/column cells.
- Added inline Natural Wonder image row/column controls with a PCX cell picker that respects custom sprite sizes.

* RELEASE 27.9
- Added Scenario-mode notices for Resources and Units when the icon index picker is using the default `resources.pcx` or `units_32.pcx`, with a one-click action that stages copying the atlas into the scenario and lists the new file in the save details.
- Added a `Save as` filename control for staged Scenario-mode leader animation replacements, matching the static art replacement rename workflow while keeping outputs under `Art\Flics`.
- Fixed Unit FLC previews showing at the wrong speed (too quickly).
- Fixed preview/debug log context so Scenario-mode art preview entries keep the active scenario BIQ in the log prefix instead of falling back to `Standard Game` / `conquests.biq`.
- Added a Units `Availability by Civ` modal for viewing and editing which units a selected civilization can build through the existing `Available To` bitmasks, with thumbnail civ picking and compact unit rows.
- Moved Civs leader animation paths into the renamed `Civilization Art and Animations` section below the leaderhead and advisor portrait art slots.
- Added lazy read-only Civs leader animation previews for the BIQ era forward/reverse FLC paths, resolved through scenario and base Civ3 art roots.
- Added Scenario-mode Civs leader animation replacement from FLC, PNG, JPEG, or PCX sources, validating standard 200x240 leader FLCs and generating repeated-frame FLCs for still images under scenario `Art\Flics`.
- Changed generated still-image leader FLCs to use Conquests-style headers and no-op `DELTA_FLC` repeat frames instead of repeated full-frame `FLI_COPY` chunks, matching Civ3 leaderhead playback expectations.
- Added a `Make Sheet` action for Civs `Civilopedia Icon 4` and `Advisor Portrait` art slots that turns one selected image into the repeated Civ3 portrait-sheet PCX format.
- Kept Civ advisor portrait sheet generation on the actual `#RACE_* line 2` slot so duplicated advisor paths in `#ICON_RACE_*` are rewritten in place instead of adding extra top-row icon frames.
- Normalized Civ `#ICON_RACE_*` blocks to the two Civilopedia icon paths on load/save so stale leaderhead, advisor, and work-folder lines do not render as extra icon cards.
- Restored Civ `Civilopedia Icon 3/4` display as synthesized views of the `#RACE_*` leaderhead and advisor lines without persisting those portrait paths inside `#ICON_RACE_*`.
- Restored `Make Sheet` on the Civ `Civilopedia Icon 4` card with tooltip text explaining the generated 461x346 repeated portrait sheet.
- Fixed Civ `Civilopedia Icon 4` editing so the `#ICON_RACE_*` slot and `#RACE_* line 2` advisor portrait remain separate while both can generate 461x346 sheets.
- Adjusted generated Civ repeated portrait sheets to use 115px circular portraits with 1px spacing where the 461x346 sheet allows, and use magenta (`#ff00ff`) in PCX palette index 255 for the sheet background.
- Matched generated Civ repeated portrait sheet mask placement and curvature to vanilla advisor PCX sheets.
- Fixed narrow Civ art editor layouts so sheet previews and art action buttons stay inside their cards instead of overflowing under the sidebar.
- Fixed Scenario-mode art selection from scenario-local work folders such as `Art-dev` so chosen files display and save under the slot's expected Civ3 art folder instead of keeping a reference to the source folder.
- Added an `All Civs` option inside Units `Available To` add-item picker so every civilization can be added without extra panel controls.
- Added Scenario-mode quality warnings when BIQ-backed Improvements fall back to base `PediaIcons.txt` because the scenario-local file is missing the required `#ICON_BLDG_*` entry.
- Added a warning action labeled with the exact missing `#ICON_BLDG_*` block that stages the block as a normal pending scenario `PediaIcons.txt` edit for the existing Save workflow.

* RELEASE 27.8
- Removed the duplicate Civs `Identity > Civilization Name` editor because the top `Name` field already edits the same RACE civilization-name value.
- Fixed the Civs top `Name` editor so it saves to the RACE civilization-name field instead of the leader-name field.
- Fixed Civ name-list editing so city, military leader, and scientific leader list row edits and deletions save the correct BIQ list counts, with coverage that deleted city names stay deleted after reload.
- Fixed pending new BIQ reference entries so Tech/Civ reference pickers predict their appended BIQ index instead of temporarily using the top UI row index, preventing a newly added tech from appearing in existing civilization starting-tech slots before save.
- Fixed BIQ fixed-width text serialization so exact-width Civilopedia keys still include a null terminator, preventing Civ III from reading into the next field and requesting bogus PediaIcons entries such as a trailing extra character.
- Fixed Civilopedia art replacements so the left-side entry thumbnail updates from the pending uploaded art immediately instead of going blank until save.
- Fixed Civilopedia art upload resizing so rectangular Tech and Civ icon sources are center-cropped to Civ3's square PCX sizes instead of being padded with transparent magenta side bands.

* RELEASE 27.7.1
- Fixed C3X name/value list parsing so single-entry values like production perfume, unit limits, building prerequisites, and generated resources stay synchronized between convenience controls and the C3X tab.
- Fixed PNG-to-PCX Civilopedia art conversion so large unit icons preserve subject colors instead of letting white/gray backgrounds dominate the palette, and semi-transparent pixels are flattened before quantization.

* RELEASE 27.7
- Added compact `Next warning` jump controls to item-list tabs with quality-check warnings so users can select and scroll to the next warned entry without hunting through the list.
- Added C3X-backed `Production Perfume`, `Unit Limits`, and `Building Prerequisites` controls to the Units prerequisites board.
- Reserved PCX palette slot 254 for green transparency and slot 255 for magenta when converting true-color art to indexed PCX.
- Added an inline `Save as` filename field for pending Civilopedia art replacements so scenario-local PCX destination names can be changed before saving.
- Improved PNG/BMP/JPEG/WebP to PCX conversion for no-interpolation scaling and optimum-palette/no-dither indexing, using nearest-neighbor resizing and an image-derived palette instead of a fixed RGB cube.
- Added C3X-backed `Generated Resources` and `Production Perfume` controls to the Improvements top board, with tooltips clarifying that edits save to the C3X base config instead of the BIQ.
- Matched the Improvements top-board requirement and C3X controls to the wrapped, equal-width card styling used by the Techs top board.
- Matched the Units prerequisite, upgrade, class, and C3X controls to the same wrapped card styling.

* RELEASE 27.6
- Removed misleading Improvements `Tech Notes` summaries derived from Civilopedia prose and cleaned Civ3 `$LINK` markup when parsing Civilopedia display text.
- Added an Improvements top-board link to matching Wonder District entries, including the Wonder District thumbnail and direct navigation to the linked section.
- Added the BIQ resource `icon` field to the structured Resource Icons editor as a `resources.pcx` map icon index picker.
- Fixed district, wonder district, and natural wonder image path edits and quality checks so saved values are district-art filenames instead of full paths, while existing scenario/C3X art no longer reports false missing-art warnings.
- Fixed current district, wonder district, and natural wonder quality checks to honor the same seasonal `Art/Districts/Summer/1200` C3X fallback used by previews.
- Fixed Scenario-mode PCX art replacement so correctly sized indexed PCX files are copied byte-for-byte even if stale staged conversion data exists.
- Fixed Scenario-mode replacement paths for nonstandard PediaIcons art lines so civ leaderhead/advisor entries keep their original art folders while improvement building and wonder splash art still use their fixed target folders.
- Fixed Scenario-mode Wonder Splash replacement so uploaded splash art is staged under `Art\Wonder Splash\` instead of the Civilopedia building icon folder.
- Improved PCX resize quality by using area averaging when downscaling and bilinear sampling when upscaling staged scenario art.
- Fixed drag-and-drop art replacement under Electron context isolation by resolving dropped file paths through the preload bridge.
- Fixed Scenario-mode PCX staging so selected PCX files are resized to the target art slot dimensions when needed instead of being copied at the wrong size.
- Fixed Civilopedia art preview and save handling for 24-bit three-plane PCX files from tools like GIMP by previewing them correctly and converting them to Civ3-compatible indexed PCX files on scenario save.
- Added Scenario-mode image conversion for structured art replacement so PNG/BMP/JPEG/WebP selections are resized to the expected Civ3 art dimensions and saved as PCX files.
- Fixed Scenario-mode art replacement so newly chosen PCX files immediately display the scenario-local destination path, preview from the pending source before save, and still copy the selected source file into the scenario on save.
- Added hover help for Improvements fields, including BLDG production bonus, requirements, veteran unit, trade, happiness, food, science, and category tooltips.
- Added a structured Improvements `Building Art` editor with explicit `SINGLE`, `ERA`, and `CULTURE` modes, city icon index editing, mode-aware large/small art slots, and wonder splash editing while preserving the large sidebar Civilopedia art preview.
- Added a structured Technologies `Technology Icons` editor that labels the Civ3 `#TECH_*_LARGE` and `#TECH_*` PediaIcons targets explicitly while keeping the large sidebar tech art preview.
- Added a structured Units `Unit Art and Animations` editor that puts large/small Civilopedia icons and the existing `units_32` icon index preview above the unit animation controls.
- Added a structured Civs `Civilization Art` editor that separates Civilopedia race icons from the `#RACE_*` leaderhead and advisor portrait paths.
- Added structured Resources and Governments icon editors that label the large/small `#ICON_GOOD_*` and `#ICON_GOVT_*` Civilopedia art slots explicitly while preserving the large sidebar preview.
- Fixed the Improvements `Building Art` `CULTURE` mode rows so they use Civ3 culture groups instead of the unrelated culture-impression rules section.
- Fixed Technology PediaIcons saves to keep the app model large-first while writing Civ3's small icon to `#TECH_*` and large icon to `#TECH_*_LARGE`.
- Fixed Scenario-mode Technology icon saves so uploaded art outside the scenario is copied into `Art\tech chooser\Icons\` and PediaIcons is rewritten to scenario-local paths.
- Fixed Scenario-mode Unit Civilopedia icon saves so uploaded large/small PediaIcons art outside the scenario is copied into `Art\Civilopedia\Icons\Units\` and rewritten to scenario-local paths, leaving animation folders and the `units_32.pcx` atlas as separate unit-art concerns.
- Fixed Scenario-mode Civ art saves so uploaded Civilopedia race icons, leaderhead portraits, and advisor portraits are copied into their distinct scenario art folders and rewritten to scenario-local paths.
- Fixed Scenario-mode Resource and Government icon saves so uploaded art outside the scenario is copied into `Art\Civilopedia\Icons\Resources\` or `Art\Civilopedia\Icons\Governments\` instead of the generic icons folder.
- Fixed Scenario-mode Improvement art saves so uploaded building icons and wonder splash files outside the scenario are copied into `Art\Civilopedia\Icons\Buildings\` and PediaIcons is rewritten to scenario-local paths.
- Fixed Improvements category changes so switching an entry to Wonder or Small Wonder immediately shows the Wonder Splash selector in `Building Art`.
- Fixed Improvement PediaIcons round-tripping so era/culture-specific building icon blocks preserve every positional art path, including intentional duplicate paths used by vanilla buildings such as Granary.

* RELEASE 27.5
- Added optional daily debug log files, enabled by default, with grouped File menu controls for toggling logging, choosing/resetting the log folder, and opening the log folder directly.
- Renamed the Units `Capacity` statistic label to `Transport Capacity`.
- Added compact `Remove All` actions to Units list editors for Unit Abilities, Available To, and Stealth Attack Targets.
- Added C3X base manifest coverage for newly shipped default keys, including expanded unit stat display toggles, resource perfume, battle-created-unit spawning, impassable-tile barbarian placement, preplaced scenario leader initialization, and the corrected Great Wall impassable key spelling.
- Fixed the Units rules layout so non-checkbox Special Orders fields render with their proper editors, restoring the `Enslave Results In` unit picker and `Telepad Range` numeric field.
- Fixed localized C3X sectioned config files, including Districts, Wonder Districts, Natural Wonders, and Tile Animations, so they use the text-file encoding detector/override on load and preserve the detected encoding on save instead of forcing UTF-8.
- Fixed Improvement/Small Wonder PediaIcons saves so building icon blocks preserve the `SINGLE` marker, in-city icon index, large Civilopedia icon, small Civilopedia icon, and wonder splash art instead of degrading to generic path-only blocks.
- Changed Civilopedia and PediaIcons text saves to insert new autogenerated sections before terminal markers such as `#EOF` and `#END CIVILOPEDIA ART`.
- Added quality-audit warnings for case-mismatched Civilopedia `$LINK` targets using the raw mixed-case source keys, since Civ3 resolves those links case-sensitively.
- Added red wavy Civilopedia preview highlighting and a per-link `Fix` action for safe case-only `$LINK` target repairs.

* RELEASE 27.4
- Refactored compact boolean styling to use shared CSS tokens across Units, Improvements, Techs, and Civs while preserving the existing per-screen selectors and layout hooks.
- Matched the compact Improvements and Units boolean toggles so inline checkbox rows now use the same smaller font and checkbox sizing as the denser rules layout.
- Added shipped C3X base key `exclude_passengers_from_stealth_attack` to the manifest-backed C3X editor, quality audit coverage, and audited default-config fixture so stock newer defaults no longer warn as unknown.

* RELEASE 27.3
- Added the missing shipped R27 C3X base keys `reformat_turns_remaining_on_domestic_advisor_screen`, `patch_passengers_out_of_order_on_menu`, `can_bombard_only_sea_tiles`, `allow_corruption_in_capital`, `special_capital_decorruption_effect`, `override_no_ai_patrol`, and `override_barbarian_activity_level_for_scenario_maps` to the manifest-backed base editor, quality checks, and audited default fixture.
- Fixed the district/wonder audit validator so `buildable_without_removal = marsh` is recognized as valid instead of incorrectly warning about an unknown list value.

* RELEASE 27.2
- Restyled shared search-field active borders to use the app theme accents with a brighter focus ring and subtle glow instead of the old flat gray focus treatment.
- Tuned the shared search-field rest state back toward the previous low-border look and moved the focus emphasis inward so the active edge no longer gets clipped at container boundaries.
- Balanced the spacing around shared compact collapsible stacks so the gap below sections like `Other Art` matches the gap above `Civilopedia`.
- Normalized compact collapsible section spacing so shared panels like `Civilopedia`, `Other Art`, and `Animation` use the same gap and visual rhythm across tabs.
- Changed `Resources -> Type` to use the same segmented choice UI style as `Improvements -> Category` instead of a dropdown.
- Removed the extra Resources rules wrapper/header so the tab no longer shows a `Rules` card title or the `From BIQ section: GOOD` subtitle above its BIQ field groups.
- Changed the Resources tab so its Civilopedia section now appears in a collapsible panel instead of always being expanded.
- Removed the extra top gap above the `C3X` tab search bar so the base-config editor header aligns with the other editor tabs.
- Reworked the Civs tab into a denser responsive dashboard layout with compact identity/personality/free-tech boards, matrix-style traits and governor controls, a smarter multi-column resize model like Techs and Improvements, the same optional right-hand sidebar toggle, and a collapsible Civilopedia section.
- Reworked the Techs tab into a denser dashboard layout with a compact top board for era/cost/prerequisites/tree controls, matrix-style capability groups, the same optional right-hand sidebar toggle used by Units and Improvements, and a collapsible Civilopedia section while keeping the sidebar visible by default.
- Reworked the Improvements tab into a first-pass dense dashboard layout with grouped columns, more compact top metadata, and the same optional right-hand sidebar toggle used by the Units tab; the sidebar still defaults to visible for Improvements.
- Refined the native search-bar clear `X` hover treatment to use a rounded background highlight instead of filter-only feedback, making the affordance feel more button-like.
- Styled the native search-bar clear `X` affordance with a pointer cursor and hover/press feedback so clearing search feels more obviously clickable.
- Matched shared search-bar typography to the main tab labels so search inputs now use the same family, size, and weight style as tabs like `C3X`, `Techs`, and `Civs`.
- Removed the remaining explanatory `hint` text above the search bars in the `C3X`, `Districts`, `Wonder Districts`, and `Natural Wonders` tabs to keep those editor headers tighter.
- Removed the tab-level header strip above editor search bars, including duplicated `effective`, `editable`, and similar status pills, and pinned those search/action rows directly to the top of each tab instead.
- Deepened the Scenario mode background tint slightly in the upper-right so it better matches the visual strength of the Standard Game mode backdrop.
- Moved `Districts`, `Wonder Districts`, and `Natural Wonders` `+ Add` and `Delete` actions into the same inline search-row control area used by Civs, Techs, Units, and other entity editors.
- Reworked the top header into a single inline title-and-mode-controls row, removed the separate mode status strip, and folded the app version into the lower-left credit line as a compact badge.
- Fixed scenario `PediaIcons.txt` tech icon saves to write Windows-style relative paths like `Art\tech chooser\Icons\...`, and to strip leaked source scenario folder prefixes such as `Eldorado5\` that could crash Civ III during load.
- Fixed scenario unit INI animation saves to rewrite scenario-root or imported `Art\Units\...` references into valid Windows-relative animation paths, preventing leaked scenario prefixes and slash-style breakage in saved unit entries.
- Removed the redundant top-center scenario name chip so loaded scenarios are shown only in the scenario mode selector instead of being duplicated in the header.

* RELEASE 27.1
- Adjusted `C3X -> Unit Limits` row spacing to use the same card-style item layout as `Production Perfume`, making those structured entries visually consistent.
- Removed the hover tooltip from Districts tab entries in the left-hand district list so hovering those rows no longer shows source/file metadata.
- Improved C3X base-tab responsiveness in large scenarios by moving continuous base-field typing onto the existing grouped undo-session path instead of taking a full editable-bundle snapshot on every keystroke; dirty tracking, save output, and discrete undo actions still use the same save/restore pipeline.
- Changed the C3X base tab to construct offscreen row editors lazily as they approach the viewport, reducing initial tab-open cost on large scenario configs without changing validation, save, or undo state tracking.
- Added an Era dropdown next to search in the Techs and Units tabs, populated from the loaded scenario/base-game ERAS data and off by default; Units filter by the era of their required tech.
- Added localized encoding support for Civilopedia/PediaIcons/diplomacy text files and BIQ-backed names/labels, including auto-detection for common Civ3 code pages, same-encoding save preservation, and a global UI override when auto-detect is ambiguous.
- Unit and improvement prerequisite resource dropdowns now include bonus resources instead of restricting those BIQ pickers to luxury and strategic resources.
- Reduced a renderer leak that could accumulate global click handlers while reopening Civilopedia editors and picker widgets, which could lead to random white-screen renderer failures during typing/clicking/dragging on Windows.
- Added explicit renderer crash and hang logging plus a recovery message when the Electron content process dies, so white-screen failures are now easier to diagnose.
- Changed Civilopedia editing so live preview stays on while typing, but undo now groups the entire edit session into a single change when you click `Done`, instead of capturing every keystroke as a separate undo step.
- Fixed undo dirty-badge restoration so reverting a Civilopedia or other grouped edit now fully clears the entry/tab modified badges when the restored data matches the clean state.
- Reduced tech tree drag-time rendering overhead by throttling node movement updates to animation frames and caching node metrics for edge routing, which should make dragging smoother and reduce white-screen renderer failures on large trees.
- Reworked other continuous text/number editors, including BIQ rule fields and diplomacy editors, so undo snapshots are grouped by edit session instead of being recorded on every keystroke.

* RELEASE 27.0
- Changed `New -> Create from Base Game` to use a typed `Search Folder` field instead of a browse-only folder path, matching the copy-scenario flow and defaulting that field from the scenario name.
- Fixed base-scenario creation so pre-R28 C3X installs no longer fail when `default.tile_animations.txt` is absent; tile animations are now treated as optional during scenario creation.
- Improved scenario creation/save-as failure feedback so users now get an in-app error status, failure toast, and details summary instead of a silent modal close when creation fails.
- Fixed `C3X -> Production Perfume` so the type dropdown only offers the documented `Improvement` and `Unit` options; unknown existing names are still preserved/editable through the picker.
- Fixed `Building Prereqs for Units` parsing so quoted multi-word unit names like `"Supply Barge"` and `"Dark Supply Barge"` no longer get collapsed into comma-joined tokens in the UI.
- Tightened several structured C3X base editors so `Unit Limits` now uses a denser inline row layout, `Building Prereqs for Units` better visually nests units under their building, and `Buildings Generating Resources` now uses the same friendly labeled flag chips as district generated resources.
- Fixed district preview invalidation when switching game/mod context so the initial district art no longer reuses stale art from the previous config until another picker click.
- Fixed district representative preview initialization so era/building picker highlights now stay in sync with the initially shown district art, including suppressing the transient flash from default picker states.
- Added `render_strategy = by-building` district preview support so the preview composites base art with multiple selected building layers and the Buildings picker becomes multi-select in that mode.
- Added the remaining documented R27 district fields to the structured district editor, including `render_strategy`, `generated_resource`, `resource_prereq_on_tile`, `img_column_count`, `ai_build_strategy`, ally-build flags, impassibility flags, irrigation-source, and one-turn healing.
- Fixed district `generated_resource` and `resource_prereq_on_tile` serialization/parsing parity with C3X, including whitespace-separated generated-resource flags and correct quoting for multi-word resource names.
- Improved district generated-resource editing so the UI now uses a real resource picker plus structured `Local`, `Yields`, and `No Tech Requirement` toggles instead of raw freeform flag text.
- Added broad district config parity coverage and district preview-selection regression tests for district field round-tripping and representative building-button normalization.
- Fixed C3X bitfield-list base settings such as `special_zone_of_control_rules`, `special_defensive_bombard_rules`, `land_transport_rules`, and `special_helicopter_rules` to serialize in C3X-compatible whitespace-delimited bracket form instead of comma-separated lists.
- Added an explicit audited manifest for all 240 shipped C3X base keys, including typed family metadata for enums, bitfield lists, quoted reference lists, quoted strings, and other structured base-field serializers.
- Added stronger C3X base audit coverage so tests now verify full shipped-key manifest coverage plus save/reload syntax parity for every explicit C3X string-family field.
- Fixed C3X quoted unit/improvement reference-list settings such as `ptw_like_artillery_targeting`, `exclude_types_from_units_per_tile_limit`, `limit_defensive_retreat_on_water_to_types`, and `ai_multi_start_extra_palaces` to use C3X-compatible whitespace-delimited quoted list syntax instead of comma-separated lists.
- Fixed additional C3X enum parity issues, including the missing `if-not-slower` and `if-fast-and-not-slower` retreat-rule options and the source order for `double_minimap_size`.
- Added scalar-family C3X base audit coverage so every boolean and integer base key now has explicit save/reload syntax tests, and added an audit-tier completeness test so every manifest key must belong to a named audit bucket.
- Unified segmented chip controls across the app so shared single-choice and multi-choice chip UIs now consistently use the same green active styling.
- Added a scenario tech tree civilization filter with civilization thumbnails so modders can preview civ-specific research trees that depend on hidden free-tech gates.
- Expanded and refined the tech tree modal layout, including larger presentation, tighter hint text, a default `All Techs` filter on fresh open, and no horizontal scrollbar.
- Fixed tech tree undo behavior so `Undo` correctly restores node positions, the restore overlay appears above the tech tree modal, and the toolbar now includes both `Undo` and `Undo All` without clearing the active civ filter during undo refreshes.
- Added an optional civ-filtered non-era lane in the tech tree so faction gate techs with `era = None` can be shown separately from the visible era graph, including Tides of Crimson-style civ-specific free-tech seeds such as `BREHTONS` and `GOOD-NEUTRAL`.
- Refined the tech tree split layout so the visible era graph stays in the right pane, the left pane uses the same title treatment as the era label, and non-era techs are distributed within the visible era's vertical span instead of clumping at the top.
- Fixed multiple tech tree regressions while adding the non-era lane, including proper `None` era classification, safer gutter graph traversal, and explicit lane toggling behavior.
- Tests: pinned `c3xBaseManifest` coverage to a checked-in default base config fixture so manifest audits no longer depend on the local sibling `C3X_Districts` install contents.
- Scenario loads now kick off a background quality-audit worker that surfaces file/asset warnings without delaying the initial render, including proactive day/night terrain and district hour-art validation.
- Added background quality-audit checks for missing on-disk reference art in the Civilizations, Technologies, Resources, Governments, Improvements, and Units tabs.
- C3X tab field search now also matches each setting's release badge, so queries like `r26` find fields introduced in that release without losing normal name/description search.
- Added configurable text-file encoding support for Civilopedia, PediaIcons, and diplomacy assets, including auto-detection for common Civ3 code pages, source-encoding preservation on save, and a global UI override for localized installs/mods.
- Added first-pass non-blocking config lint checks for unknown keys and invalid boolean/integer/select/list values in C3X base config plus Districts, Wonder Districts, and Natural Wonders.
- Fixed several C3X base-field editor mismatches with the actual parser, including proper `limit_railroad_movement` editing, preserving empty bitfield-list selections, and recognizing special limit fields as non-boolean.
- Added a `File -> Settings -> Run Quality Checks After Load` preference, enabled by default, so users can disable the background post-load audit warnings if they prefer.
- Moved `Text File Encoding` out of the top file-path panel and into `File -> Settings` so file-path controls stay focused on location selection.
