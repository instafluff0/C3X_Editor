* RELEASE 27.11
- Fixed BIQ map tabs to retain raw BIQ ownership/reference values from the underlying section data and serialize those raw values instead of enriched UI labels, eliminating no-op map rewrites, correcting player-owned city/unit reload behavior, and making painted scenario districts persist to `scenario.districts.txt` instead of staying as map-only visuals.
- Fixed scenario saves omitting the BIQ `map` tab from the save payload, so Map editor BIQ changes and `scenario.districts.txt` writes now persist.
- Changed BIQ map unit overlays to choose the visible unit by tile context and defense strength: strongest land defender on land, strongest sea defender on water, then air or next strongest fallback, with Tile Info unit add/remove redraw checks using the same chooser.
- Added a default-on `File -> Settings -> Map -> Auto-Dock Tile Info Left Near Right Edge` toggle so Tile Info only flips to the left near the extreme-right map edge when that behavior is enabled.
- Unified BIQ map district building-influence rendering and city-triggered district refreshes onto the same dynamic `city_work_radius` footprint used by wonder-district nearby-city assignment.
- Added a right-click Map editor wonder-district completion menu in both Select and Paint modes, fed by nearby completed wonders from the current dynamic `city_work_radius`, and aligned wonder-district sidecar writes so incomplete vs completed map art stays in sync.
- Added `Ctrl+Z` / `Cmd+Z` support in the Map modal so the shortcut triggers the same scoped map Undo action as the toolbar button.
- Updated Tile Info `Units` add-picker count suffixes to read as totals, e.g. `(3 total)`.
- Tile Info `Units` add pickers now show each available unit type's current count for the selected civ as a subtle suffix in the dropdown list.
- Added map and trash icons to the Map tab `Open Map` and `Remove Map` actions so the buttons read more clearly at a glance.
- Fixed flat-terrain paint previews in the Map editor so Terrain hover previews no longer draw half a tile too high relative to the selected Civ3 diamond.
- Changed Map editor Select-mode Tile Info `Units` owner fallback so empty tiles in owned territory now default the owner picker to the territory owner before using the generic remembered/default owner.
- Fixed Map editor landmark terrain rendering so LM-tagged base tiles now load and draw from the full landmark terrain PCX family (`lxtgc/lxpgc/lxdgc/lxdpc/lxdgp/lxggc/lwCSO/lwSSS/lwOOO`), while hover previews also use the landmark flat/tall art variants.
- Fixed temporary Ctrl-based Zoom/Pan in the Map editor so Select-mode zoom rerenders no longer drop back out of move mode while Ctrl is still held.
- Added Map editor wonder-district completion controls in the Tile Info modal, with nearby-city wonder assignment stored to `scenario.districts.txt` and immediate construction-vs-completed wonder art refresh on the map.
- Fixed a Map editor modal startup regression where barbarian ownership helpers were referenced before initialization during map-open territory setup, causing `Cannot access 'getBarbarianCivilizationPickerEntry' before initialization`.
- Added internal padding to the Map editor Select-mode empty `Cities` Tile Info message so `No city on this tile` no longer sits tight against the card edges.
- Fixed barbarian-owned unit stacks on the BIQ map so their on-map civ dots and recolored unit art use barbarian colors instead of falling back to player/civ red, and corrected structured owner writes to use the BIQ barbarian owner type.
- Fixed Map editor Paint-mode overlay art so overlay picker thumbnails and drag previews now trim transparent atlas padding for small/tall sprites like victory points, outposts, colonies, and radar towers, while map rendering now places colony-like overlays lower, restores barricade rendering, and shifts ruins down/right to match Civ3 tile anchors better.
- Simplified Map editor Paint-mode toolbar checkbox styling by removing the pill-outline treatment from terrain-variant and display toggles, and removed the `No variants` placeholder text for terrains that have no variant controls.
- Added the barbarian civilization to the Map editor Select-mode structured `Owner` pickers for cities and units when that BIQ exposes one, with matching write/tint behavior so barbarian ownership can be selected directly.
- Fixed Map editor modal auto-restore so persisted Paint-mode terrain variant controls now appear immediately on reopen/startup, instead of staying hidden until another toolbar interaction refreshed the chrome.
- Fixed Map editor Select-mode empty `City` and `Units` Tile Info cards so they clear any stale civ tint and return to the neutral panel background when the selected tile has no city or no units.
- Fixed Map editor modal Paint-mode `Show Grid` and `Show Landmarks` toggles so they now refresh the canvas in place instead of rebuilding the modal body, preventing terrain-variant controls like Forest `LM` and `Pine Forest` from disappearing after display-toggle changes.
- Fixed Map editor Select-mode Tile Info left-dock persistence across ownership-triggered full map rerenders, so a left-docked inspector no longer jumps to the right and animates back left after city-owner changes.
- Fixed Map editor city and city-linked unit ownership edits so territory border colors and city civ-color overlays now refresh immediately on the map when ownership changes.
- Changed Map editor Select-mode unit ownership edits so changing units on a city tile now also transfers that tile's city owner, keeping tile ownership in sync automatically.
- Slowed and softened the Map editor Select-mode Tile Info side-swap animation, and made the panel go slightly translucent while it glides between the right and left dock positions near the far-right map edge.
- Changed Map editor Select-mode city ownership edits so changing a city's owner now also transfers any units already on that same tile to the new owner automatically.
- Extended the Map editor Select-mode Tile Info civ-color wash to the `City` category, so tiles with a city now tint the whole panel from that city's owner in the same subtle style as `Units`.
- Stopped the Select-mode Tile Info category dropdown from forcing redundant map/Tile Info refresh work; it now follows the same fog-aware redraw rule as the Paint toolbar category picker.
- Strengthened the Map editor Select-mode Tile Info `Units` owner tint so the civ-colored panel wash now visibly survives the shared card styling instead of getting flattened back to the default background.
- Stopped unnecessary full-map redraws when switching Map editor Paint/Select categories; category changes now only repaint the canvas when they enter or leave the Fog of War overlay state.
- Changed Map editor Select-mode Tile Info `Units` owner tinting so the civ color now softly washes the full Tile Info panel background, and the add-unit box no longer gets a colored border.
- Tightened the Map editor Tile Info `Units` add-unit picker class bias so terrain-based sea/land favoritism now applies only to unit types that already appear more than once for the selected owner on the map, with the same rule mirrored on land tiles in reverse.
- Fixed the Map editor Tile Info `Units` add-unit picker water-tile sort so naval preference is now applied before frequency ranking, keeping sea units ahead of land units instead of only breaking ties between equally common entries.
- Added `None` entries to the Map editor Paint-mode Overlay and District pickers, with clear-glyph previews and erase behavior so painting with those options removes all overlays or clears districts from the brushed tiles.
- Refined the Map editor Tile Info `Units` add-unit picker so its live owner-frequency ranking now refreshes as the map changes and water tiles prefer sea units over land units while keeping the existing frequency-first ordering.
- Fixed the Map editor Tile Info `Units` add-unit picker so stale remembered selections no longer keep forcing Worker; unless the user explicitly picks a unit, the control now reinitializes from the current ranked default for that owner/tile.
- Fixed the Map editor Tile Info `Units` add-unit picker so its initial selection no longer inherits the top-bar paint unit choice; it now defaults from the Tile Info ranked unit list instead of commonly snapping to Worker.
- Changed the Map editor Tile Info `Units` add-unit picker to rank units by how often the selected owner already uses them on the current map, then fall back to native units-tab order for unused entries, while filtering out non-era units like the Availability by Civ modal does.
- Increased the Map editor air-unit overlay offset again so aircraft render lower within their tiles without changing land or naval placement.
- Tuned the new Map editor non-land unit overlay offsets so naval units render a bit lower than before and air units render much lower, while land-unit placement stays unchanged.
- Fixed Map editor Tile Info owner and unit picker dropdowns so they now render in a true floating layer above the modal instead of being clipped to the Tile Info inspector card.
- Adjusted Map editor sea and air unit overlay placement so naval and air units render lower within their tiles, using a zoom-scaled vertical offset while keeping land-unit placement unchanged.
- Fixed the Map editor Select-mode Tile Info `Units` type picker so its dropdown opens reliably inside the map modal again, by keeping the floating menu within the modal stack and stopping picker clicks from leaking into parent handlers.
- Simplified the Map editor Tile Info `Units` add-unit chooser overlay so it now uses a fixed `document.body` menu path instead of the earlier custom portal host logic that could leave the dropdown invisible.
- Optimized Map editor Tile Info `Units` edit latency further by replacing per-click map undo snapshots with grouped per-tile unit edit sessions, tightening single-unit delete bookkeeping to update only the affected tile link, and extending timing logs so unit mutation cost is separated cleanly from redraw cost.
- Fixed the Map editor Tile Info `Units` add-unit dropdown overlay stacking so the portaled chooser now renders above the map modal instead of opening invisibly behind it.
- Removed the full-map rerender path from Map editor Tile Info `Units` edits: add/remove/owner/experience changes now refresh Tile Info immediately and only redraw the selected map tile when the visible unit overlay actually changes, with new timing logs for each unit edit and refresh decision.
- Fixed the Map editor Tile Info `Units` add-unit dropdown so it now opens as a true floating overlay anchored to the map frame, restoring the inspector’s normal scroll behavior instead of blanking panel content or stretching the Tile Info box.
- Fixed the Map editor floating Tile Info inspector so the `Units` add-unit picker can overflow outside the panel without blanking the unit stack or triggering inspector scrollbars.
- Fixed the Map editor Tile Info `Units` add-unit picker so its dropdown now opens as a floating menu above the Tile Info stack instead of expanding that panel and triggering unwanted scrollbars.
- Fixed Map editor Tile Info `Units` add-panel owner border colors so they now resolve through the same player/civilization color-slot mapping as city borders and territory edges, instead of incorrectly using the raw player index as a civ color slot.
- Tightened Map editor Select-mode Tile Info `Units` row styling so larger stacks fit more cleanly: unit names now use a smaller single-line ellipsis layout, row padding is reduced, and the experience/remove controls take less vertical space.
- Made the Map editor Tile Info `Units` add panel border adopt the selected owner civilization's color, matching the map's territory color cues while keeping the existing 1px outline.
- Changed the Map editor Tile Info `Units` add action to use the app’s standard `secondary` button styling so it now matches controls like `Open Map` instead of using custom button visuals.
- Tightened the Map editor Tile Info `Units` add button sizing so it flexes within the available row space without forcing horizontal overflow, and matched its corner radius to the surrounding controls.
- Restyled the Map editor Tile Info `Units` add action into a wider blue primary button so the `Add` control reads clearly instead of inheriting the tiny circular list-button styling.
- Compacted the Map editor Tile Info `Units` add row by removing the visible `Experience` and `Qty` labels and turning the small `+` control into a full-width `Add` button with an icon.
- Changed Map editor Select-mode Tile Info `Units` owner control to the structured civilization picker used by cities, so it now shows owner thumbnails and still handles mixed-owner stacks cleanly.
- Fixed persisted BIQ map sessions so if the app is closed while the Map editor modal is open, restarting on the `Map` tab now reopens the modal automatically instead of requiring another `Open Map` click.
- Fixed Map editor Select-mode Tile Info `Units` owner dropdown hydration so clicking a tile with existing units now resolves and displays the owning civilization/player correctly, including BIQs that store unit owners through civ ownership data instead of direct player indexes.
- Fixed the Map editor City `Owner` row so only the picker itself opens, instead of the whole labeled wrapper behaving like a clickable form label.
- Fixed Map editor `Remove City` using the live city record index instead of the Tile Info display string, and added delete-flow debug logging so confirmed city deletions can be traced end to end.
- Fixed Map editor `Remove City` so deleting a city from Tile Info now clears the live `TILE.city` link used by map rendering, preventing the removed city from lingering visually on the map.
- Added targeted Map editor Tile Info city logging so population/culture edits now report their input/change triggers, queued delayed rerenders, rerender execution timing, and city building toggle timings for sluggishness investigation.
- Optimized map-scoped undo capture for Tile Info city edits by storing serialized map snapshots directly, reducing the extra clone/signature churn that had been inflating per-click building-toggle latency.
- Made Map editor Tile Info city editing use a grouped active-city undo session and canvas-only refreshes for city name, population, and building toggles, so those controls no longer force a full map snapshot or full modal rerender on every interaction.
- Made Map editor `Undo` and `Undo All` apply map snapshots locally in memory instead of routing through the full scenario bundle reload path, removing the loading modal and making map-only undo much faster while keeping the modal open in place.
- Fixed Map editor modal Paint-mode drag painting so holding left mouse and sweeping across tiles now applies edits continuously, and replaced the old full modal rerender-per-tile path with clipped in-place canvas redraws plus no-op paint suppression to make terrain/overlay painting much faster.
- Added `Undo` and `Undo All` buttons to the Map editor modal header, with map-scoped undo behavior so the modal rewinds map content edits like terrain, overlays, cities, units, and tile data without undoing camera movement or tool-mode changes.
- Fixed Map editor navigation state so leaving the map for a linked reference and then using `Back` now restores the current map session instead of an older snapshot; scroll position, selected tile, zoom, modal reopen state, and display/tool settings persist correctly.
- Simplified Map editor Terrain paint previews so the hover overlay now fills the full tile diamond with the selected terrain's representative color instead of trying to predict transition graphics from the hovered tile neighborhood.
- Fixed Map editor desert-edge painting so terrain edits now repair invalid four-tile `desert + plains + grassland + coast` transition quartets by promoting the local grassland edge to plains, preventing the broken-looking desert/grassland blends that could appear after painting near coasts.
- Added `River` to the Map editor overlay tools and Tile Info overlay picker, with river edits now updating `TILE.riverConnectionInfo` reciprocally on neighboring tiles so river sprites reconnect immediately when you paint or remove river segments.
- Fixed a Map editor mode-state bug where switching from Select back to Paint could leave the brush path reading the stale generic tool mode, causing clicks to no-op even though the UI was in Paint; painting now resolves directly from the active paint category.
- Expanded Map editor debug logging around painting and redraws so logs now capture paint start/apply/end events, brush tile summaries, terrain normalization/recalc scopes, selective tile-edit redraw targets, and canvas redraw timing/clip metrics for performance debugging.
- Added a Select-mode map right-click category menu so right-clicking a tile now selects it and opens the same Tile Info category dropdown used by the inspector, letting you switch between Terrain, Overlays, Resources, and the other tile editors directly from the map.
- Changed Map editor terrain markers so Bonus Grassland always renders with the centered `tnt.pcx` overlay art, Landmark tiles use a centered `LM` marker, and the top bar now includes a global `Show Landmarks` toggle next to `Show Grid`.
- Added Map editor terrain-variant controls for Landmark terrain, Bonus Grassland, Pine Forest, and Snow-Capped Mountains in both Paint mode and Select-mode Tile Info, with shared compatibility rules so invalid variants clear automatically when terrain changes.
- Fixed Map editor floodplain painting so non-river tiles fall back to desert while river tiles write real floodplains with desert base terrain and draw the floodplain overlay art instead of looking like plain desert everywhere.
- Made Map editor tundra painting normalize adjacent plains and desert tiles back to grassland before recalculating terrain transitions, matching Firaxis's tundra-edge behavior more closely.
- Changed the Map editor modal's first-open camera target to the center of the canonical world area instead of the first tile near `0,0`, so maps open centered by default.
- Fixed non-wrapping Map editor edge terrain rendering so missing border neighbors are treated as coast during terrain-sprite selection instead of incorrectly falling back to grassland, which had created false cutoff land on maps like `6 MP Age of Discovery.biq`.
- Clamped Map editor panning to the canonical map window instead of the full padded wrap canvas, removing the upper-left overscroll gutter and aligning horizontal seam scrolling more closely with Firaxis's editor.
- Tightened the Map editor top-bar structured picker sizing so the terrain/overlay/resource/district dropdown no longer renders taller than neighboring controls or expands the toolbar row.
- Fixed slow Map editor mode-button clicks by routing the Move/Select/Paint buttons through the same optimized mode-switch path as `Tab`, instead of using an older click handler that always forced a full canvas redraw.
- Fixed slow Map editor `Tab` mode cycling by skipping the expensive full map redraw on pure interaction-mode changes that do not affect Fog of War rendering, and corrected the Paint-mode state so cycling into Paint preserves a real paint category instead of writing the invalid category value `paint`.
- Added detailed Map editor `Tab` mode-cycle timing logs so debug output now shows where time is spent across the hotkey handler, `setToolMode`, toolbar refresh, interaction overlay refresh, and in-place map redraw.
- Restored instant-feeling `Tab` mode cycling in the Map editor by preventing repeated Tab auto-cycles, intercepting the hotkey earlier, and making the mode-flash hint update without rerendering the full toolbar.
- Restored Map editor Select-mode Tile Info `Units` editing so the category again shows a real unit stack editor with owner selection, inline unit adding, experience/quantity controls, per-unit veteran dropdowns, and remove actions; the add flow now uses real BIQ unit indexes so clicking `+` immediately creates units on the selected tile and in the list.
- Restored Map editor Fog of War support after a renderer regression: Select and Paint `Fog of War` categories again use raw BIQ fog values, Paint mode exposes Add Fog / Remove Fog in the top toolbar, and gray fog overlays only draw while the active category is Fog of War.
- Restored the Map editor city Population/Culture delayed-rerender fix so each scheduled redraw preserves its own captured viewport snapshot instead of falling back to the upper-left scroll position.
- Added Select-mode Tile Info editing for starting locations, with immediate assignment updates, clearer player/civ labels, and automatic removal of any previous start already assigned to that same civilization or player.
- Added Select-mode Tile Info visibility controls that edit each tile's scenario fog-of-war state and clarify that Quint renders fogged tiles with the gray `EditFog` overlay.
- Added a confirmed `Remove City` action to the Map editor Tile Info City panel so scenario users can delete cities directly from the selected tile.
- Unified Map editor special tile labels so Named Tiles stay visible when not selected and Natural Wonder names render on-map with the same text style and offsets.
- Debounced Select-mode Named Tile typing in the Map editor and moved the selected tile's named-tile label above the highlight overlay so it stays readable while editing.
- Added Select-mode Named Tile editing in the Map editor Tile Info panel and rendered named-tile labels on the map above city names using the same badge style.
- Refined Map editor Tile Info resource counts to show as subtle second-line text under each resource name and made them refresh immediately after resource edits.
- Added resource-count labels to the Map editor Tile Info Resources picker in Select mode, so each resource option shows how many copies exist on the current map.
- Added Map editor hotkeys for `Ctrl+G` grid toggling and browser-style `Ctrl/Cmd` + `+`/`-` zoom, and exposed the grid shortcut in the `Show Grid` tooltip.
- Made Map editor modal keyboard shortcuts respond immediately on first open and removed the stray browser focus outline from the map canvas while keeping the new `Tab` mode-cycle shortcut.
- Added a `Tab` shortcut in the Map editor modal to cycle Move, Select, and Paint modes, with a bottom-hint flash that confirms each mode jump alongside the existing temporary Ctrl-to-Move hint.
- Made the Map editor modal draw real district and natural-wonder map art, using territory owner culture/era, district `render_strategy`, nearby city improvements, and resource layering instead of generic district dots.
- Changed the Map editor Paint-mode preview from a floating badge to a semi-transparent in-tile ghost, so the selected Terrain, Resource, Overlay, or District appears directly inside the hovered tile diamond.
- Added a semi-transparent Paint-mode cursor ghost in the Map editor that follows the mouse and previews the currently selected Terrain, Resource, Overlay, or District art.
- Fixed the Map editor modal Paint-mode 3x3 brush to use Civ3 logical tile coordinates, so the hover footprint and painted tiles form the intended 3-by-3 block instead of the skewed packed-record shape.
- Corrected Map editor brush-footprint math to use the Civ3-style logical tile grid, fixing 3x3 and larger Paint previews and edits that were previously too wide on the diagonals.
- Made the Map editor Paint-mode hover outline expand to the real brush footprint and renamed the toolbar label from Brush to Brush Size.
- Restricted the Map editor Tile Info panel to Select mode so Paint mode no longer leaves the inspector visible; Paint selections stay configurable from the top toolbar instead.
- Limited Map editor mouse-wheel zooming to Move mode, including the temporary Ctrl-held move state, so scrolling in other tools no longer zooms the map.
- Removed the redundant Add/Erase checkbox from the Map editor Paint toolbar and made Paint mode always apply the selected Terrain, Overlay, Resource, or District when you click a tile.
- Switched the Map editor Paint toolbar's Resource picker to the same full-size resource thumbnails used in Tile Info so icons no longer upscale from tiny pixelated sprites.
- Fixed structured picker option normalization so Paint toolbar overlay and district entries keep their thumbnail/jump metadata instead of dropping back to placeholder icons.
- Fixed the Map editor Paint toolbar's Overlay and District pickers to reuse the same Tile Info thumbnail previews, so roads, railroads, mines, irrigation, and district art no longer fall back to empty placeholder boxes.
- Replaced the Map editor Paint toolbar's Terrain, Overlay, Resource, and District controls with structured thumbnail pickers and added jump links into Terrain Types, Worker Jobs, Resources, and Districts where applicable.
- Tightened the Map editor Paint toolbar by switching the Paint icon away from a pencil, removing the redundant Paint label beside the category picker, and limiting paint categories to Terrain, Overlays, Resources, and Districts.
- Fixed Map modal history navigation so jumping from a map link to another tab closes the modal, and Back restores the same map session with the modal reopened.
- Made the Scenario Map tab open the full map editor immediately when the current scenario already has map data, removing the extra Open Map click.
- Made Map editor colony-family overlays use real `CLNY` records and Quint-style art, so colonies, airfields, outposts, and radar towers paint, toggle, thumbnail, and render from the correct BIQ structures and terrain PCX assets.
- Fixed Map editor overlay selectors so scenario-aware thumbnails appear on first open and terrain-PCX overlays like pollution, craters, barbarian camps, goody huts, ruins, and victory points use real map art thumbnails instead of placeholder icons.
- Loaded and rendered Map editor goody huts, barbarian camps, pollution, craters, ruins, and other scenario map overlays from the same Civ3 PCX assets and slice rules used by Quint.
- Restyled Map editor Tile Info option selectors and City improvement choices with flatter two-column rows, left thumbnails, selector markers, and Unit Availability blue active states.
- Added visual Map editor Tile Info grids for placing scenario districts and natural wonders with thumbnails.
- Made Map editor territory border comparisons use resolved civilization identity, preventing borders between cities that resolve to the same civ through different owner slots.
- Fixed Map editor territory rendering after city owner edits so reverting an owner does not leave stale borders between cities with the same resolved owner.
- Kept the Map editor City improvement list in place when toggling improvements and hid the Wealth/capitalization pseudo-improvement from city improvement choices.
- Made the Map editor City owner picker show only friendly civilization names and align its dropdown width with the picker.
- Made the Map editor City tile title inline-editable and removed the duplicate city Name field from the city controls.
- Matched City tile improvement link buttons to the app's standard rounded-square reference jump styling.
- Debounced Map editor city field redraws to preserve the viewport during rapid changes and refined City tile improvements into cohesive linked rows.
- Added visible city population and culture controls plus linked improvement-thumbnail building toggles to the Map editor Tile Info panel.
- Set Map editor Mountain and Volcano terrain paints on Grassland base terrain, matching Civ3's expected packed terrain form.
- Made Map editor terrain painting normalize coastlines so adding land in water creates a proper island with surrounding coast tiles.
- Added Select-mode map tile editors for terrain, resources, and overlays with visual option grids and scenario-aware resource art.
- Added a Select-mode tile information panel with category-specific tile details and renamed the map toolbar Grid toggle to Show Grid.
- Added a temporary Control-to-Move shortcut in the Map editor with an active bottom-center indicator and map border cue.
- Updated the Map editor Move, Select, and Paint controls to use bold app-style colors with neutral inactive buttons.
- Removed the full Map editor redraw from ordinary terrain tile selection so clicked selection feedback is immediate.
- Split Map editor tile markers so selected tiles use an outer dark purple outline while hovered tiles use an inset light purple outline.
- Fixed Map editor mode behavior so Move, Select, and Paint use distinct cursors and only Select maintains a persistent clicked tile selection.
- Made Map editor mode switches update the toolbar and transient highlights in place instead of rebuilding the full map modal.
- Simplified the Map editor modal by removing the legacy top view bar, hiding the terrain inspector, moving Grid into the mode bar, and removing the modal Remove Map action.
- Added Map editor support for C3X `scenario.districts.txt` placements, including named tiles and sidecar-backed district/natural wonder edits.
- Revamped the Map editor controls around Move, Select, and Paint modes with category-specific tool palettes and a floating tile inspector.
- Split the test suite into fast, BIQ, and full npm tiers so day-to-day verification can avoid the longest BIQ round-trip and CRUD suites while keeping them available for release checks.
- Enabled the Map tab while hiding map generation actions from the Map UI and preserving Civ3 map-art transparency slots.
- Matched Map tab resource overlays to Quint's `TILE.resource -> GOOD.icon -> resources.pcx` mapping.
- Fixed BIQ map resource overlays reading GOOD icon indices from map/scenario records so scenario fish resources no longer fall back to deer/game icons.
- Made Map tab art lookups prioritize BIQ scenario search folders so scenario-local `Art/resources.pcx` and related map assets override base game art.
- Added a Map tab hover tooltip that shows the grid coordinates under the pointer, outlines the hovered tile using Civ3's logical tile footprint, and avoids the native OS tooltip.

* RELEASE 27.10
- Added direction badges to Wonder District art previews when alternate-direction art is enabled, marking main art as right-facing and alt art as left-facing.
- Fixed the Units tab responsive layout so the right sidebar no longer overlaps the dense rules content before the content grid collapses.
- Added an Improvements `Required Districts` C3X convenience field that edits matching Districts `dependent_improvs` entries and supports multiple required districts.
- Added inline Wonder District art row/column controls with a larger, right-aligned sheet-cell selector for completed, construction, and enabled alternate-direction art coordinates.
- Fixed BIQ-backed Small Wonders so Wonder District name search uses the BIQ category flags instead of requiring matching Civilopedia/PediaIcons wonder metadata.
- Removed the placeholder `New Wonder` entry from Wonder District name pickers.
- Fixed Wonder District cell pickers so changing the image path or custom cell size reloads the picker from the current PCX.
- Added a Districts Button Tile sheet picker for selecting `WorkerDistrictButtonsNorm.pcx` row/column cells.
- Added inline Natural Wonder image row/column controls with a PCX cell picker that respects custom sprite sizes.

* RELEASE 27.9
- Added Scenario-mode notices for Resources and Units when the icon index picker is using the default `resources.pcx` or `units_32.pcx`, with a one-click action that stages copying the atlas into the scenario and lists the new file in the save details.
- Added a `Save as` filename control for staged Scenario-mode leader animation replacements, matching the static art replacement rename workflow while keeping outputs under `Art\Flics`.
- Fixed Unit FLC previews showing at the wrong speed (too quickly).
- Fixed preview/debug log context so Scenario-mode art preview entries keep the active scenario BIQ in the log prefix instead of falling back to `Standard Game` / `conquests.biq`.
- Added a Units `Availability by Civ` modal for viewing and editing which units a selected civilization can build through the existing `Available To` bitmasks, with thumbnail civ picking and compact unit rows.
- Moved Civs leader animation paths into the renamed `Civilization Art and Animations` section below the leaderhead and advisor portrait art slots.
- Added lazy read-only Civs leader animation previews for the BIQ era forward/reverse FLC paths, resolved through scenario and base Civ3 art roots.
- Added Scenario-mode Civs leader animation replacement from FLC, PNG, JPEG, or PCX sources, validating standard 200x240 leader FLCs and generating repeated-frame FLCs for still images under scenario `Art\Flics`.
- Changed generated still-image leader FLCs to use Conquests-style headers and no-op `DELTA_FLC` repeat frames instead of repeated full-frame `FLI_COPY` chunks, matching Civ3 leaderhead playback expectations.
- Added a `Make Sheet` action for Civs `Civilopedia Icon 4` and `Advisor Portrait` art slots that turns one selected image into the repeated Civ3 portrait-sheet PCX format.
- Kept Civ advisor portrait sheet generation on the actual `#RACE_* line 2` slot so duplicated advisor paths in `#ICON_RACE_*` are rewritten in place instead of adding extra top-row icon frames.
- Normalized Civ `#ICON_RACE_*` blocks to the two Civilopedia icon paths on load/save so stale leaderhead, advisor, and work-folder lines do not render as extra icon cards.
- Restored Civ `Civilopedia Icon 3/4` display as synthesized views of the `#RACE_*` leaderhead and advisor lines without persisting those portrait paths inside `#ICON_RACE_*`.
- Restored `Make Sheet` on the Civ `Civilopedia Icon 4` card with tooltip text explaining the generated 461x346 repeated portrait sheet.
- Fixed Civ `Civilopedia Icon 4` editing so the `#ICON_RACE_*` slot and `#RACE_* line 2` advisor portrait remain separate while both can generate 461x346 sheets.
- Adjusted generated Civ repeated portrait sheets to use 115px circular portraits with 1px spacing where the 461x346 sheet allows, and use magenta (`#ff00ff`) in PCX palette index 255 for the sheet background.
- Matched generated Civ repeated portrait sheet mask placement and curvature to vanilla advisor PCX sheets.
- Fixed narrow Civ art editor layouts so sheet previews and art action buttons stay inside their cards instead of overflowing under the sidebar.
- Fixed Scenario-mode art selection from scenario-local work folders such as `Art-dev` so chosen files display and save under the slot's expected Civ3 art folder instead of keeping a reference to the source folder.
- Added an `All Civs` option inside Units `Available To` add-item picker so every civilization can be added without extra panel controls.
- Added Scenario-mode quality warnings when BIQ-backed Improvements fall back to base `PediaIcons.txt` because the scenario-local file is missing the required `#ICON_BLDG_*` entry.
- Added a warning action labeled with the exact missing `#ICON_BLDG_*` block that stages the block as a normal pending scenario `PediaIcons.txt` edit for the existing Save workflow.

* RELEASE 27.8
- Removed the duplicate Civs `Identity > Civilization Name` editor because the top `Name` field already edits the same RACE civilization-name value.
- Fixed the Civs top `Name` editor so it saves to the RACE civilization-name field instead of the leader-name field.
- Fixed Civ name-list editing so city, military leader, and scientific leader list row edits and deletions save the correct BIQ list counts, with coverage that deleted city names stay deleted after reload.
- Fixed pending new BIQ reference entries so Tech/Civ reference pickers predict their appended BIQ index instead of temporarily using the top UI row index, preventing a newly added tech from appearing in existing civilization starting-tech slots before save.
- Fixed BIQ fixed-width text serialization so exact-width Civilopedia keys still include a null terminator, preventing Civ III from reading into the next field and requesting bogus PediaIcons entries such as a trailing extra character.
- Fixed Civilopedia art replacements so the left-side entry thumbnail updates from the pending uploaded art immediately instead of going blank until save.
- Fixed Civilopedia art upload resizing so rectangular Tech and Civ icon sources are center-cropped to Civ3's square PCX sizes instead of being padded with transparent magenta side bands.

* RELEASE 27.7.1
- Fixed C3X name/value list parsing so single-entry values like production perfume, unit limits, building prerequisites, and generated resources stay synchronized between convenience controls and the C3X tab.
- Fixed PNG-to-PCX Civilopedia art conversion so large unit icons preserve subject colors instead of letting white/gray backgrounds dominate the palette, and semi-transparent pixels are flattened before quantization.

* RELEASE 27.7
- Added compact `Next warning` jump controls to item-list tabs with quality-check warnings so users can select and scroll to the next warned entry without hunting through the list.
- Added C3X-backed `Production Perfume`, `Unit Limits`, and `Building Prerequisites` controls to the Units prerequisites board.
- Reserved PCX palette slot 254 for green transparency and slot 255 for magenta when converting true-color art to indexed PCX.
- Added an inline `Save as` filename field for pending Civilopedia art replacements so scenario-local PCX destination names can be changed before saving.
- Improved PNG/BMP/JPEG/WebP to PCX conversion for no-interpolation scaling and optimum-palette/no-dither indexing, using nearest-neighbor resizing and an image-derived palette instead of a fixed RGB cube.
- Added C3X-backed `Generated Resources` and `Production Perfume` controls to the Improvements top board, with tooltips clarifying that edits save to the C3X base config instead of the BIQ.
- Matched the Improvements top-board requirement and C3X controls to the wrapped, equal-width card styling used by the Techs top board.
- Matched the Units prerequisite, upgrade, class, and C3X controls to the same wrapped card styling.

* RELEASE 27.6
- Removed misleading Improvements `Tech Notes` summaries derived from Civilopedia prose and cleaned Civ3 `$LINK` markup when parsing Civilopedia display text.
- Added an Improvements top-board link to matching Wonder District entries, including the Wonder District thumbnail and direct navigation to the linked section.
- Added the BIQ resource `icon` field to the structured Resource Icons editor as a `resources.pcx` map icon index picker.
- Fixed district, wonder district, and natural wonder image path edits and quality checks so saved values are district-art filenames instead of full paths, while existing scenario/C3X art no longer reports false missing-art warnings.
- Fixed current district, wonder district, and natural wonder quality checks to honor the same seasonal `Art/Districts/Summer/1200` C3X fallback used by previews.
- Fixed Scenario-mode PCX art replacement so correctly sized indexed PCX files are copied byte-for-byte even if stale staged conversion data exists.
- Fixed Scenario-mode replacement paths for nonstandard PediaIcons art lines so civ leaderhead/advisor entries keep their original art folders while improvement building and wonder splash art still use their fixed target folders.
- Fixed Scenario-mode Wonder Splash replacement so uploaded splash art is staged under `Art\Wonder Splash\` instead of the Civilopedia building icon folder.
- Improved PCX resize quality by using area averaging when downscaling and bilinear sampling when upscaling staged scenario art.
- Fixed drag-and-drop art replacement under Electron context isolation by resolving dropped file paths through the preload bridge.
- Fixed Scenario-mode PCX staging so selected PCX files are resized to the target art slot dimensions when needed instead of being copied at the wrong size.
- Fixed Civilopedia art preview and save handling for 24-bit three-plane PCX files from tools like GIMP by previewing them correctly and converting them to Civ3-compatible indexed PCX files on scenario save.
- Added Scenario-mode image conversion for structured art replacement so PNG/BMP/JPEG/WebP selections are resized to the expected Civ3 art dimensions and saved as PCX files.
- Fixed Scenario-mode art replacement so newly chosen PCX files immediately display the scenario-local destination path, preview from the pending source before save, and still copy the selected source file into the scenario on save.
- Added hover help for Improvements fields, including BLDG production bonus, requirements, veteran unit, trade, happiness, food, science, and category tooltips.
- Added a structured Improvements `Building Art` editor with explicit `SINGLE`, `ERA`, and `CULTURE` modes, city icon index editing, mode-aware large/small art slots, and wonder splash editing while preserving the large sidebar Civilopedia art preview.
- Added a structured Technologies `Technology Icons` editor that labels the Civ3 `#TECH_*_LARGE` and `#TECH_*` PediaIcons targets explicitly while keeping the large sidebar tech art preview.
- Added a structured Units `Unit Art and Animations` editor that puts large/small Civilopedia icons and the existing `units_32` icon index preview above the unit animation controls.
- Added a structured Civs `Civilization Art` editor that separates Civilopedia race icons from the `#RACE_*` leaderhead and advisor portrait paths.
- Added structured Resources and Governments icon editors that label the large/small `#ICON_GOOD_*` and `#ICON_GOVT_*` Civilopedia art slots explicitly while preserving the large sidebar preview.
- Fixed the Improvements `Building Art` `CULTURE` mode rows so they use Civ3 culture groups instead of the unrelated culture-impression rules section.
- Fixed Technology PediaIcons saves to keep the app model large-first while writing Civ3's small icon to `#TECH_*` and large icon to `#TECH_*_LARGE`.
- Fixed Scenario-mode Technology icon saves so uploaded art outside the scenario is copied into `Art\tech chooser\Icons\` and PediaIcons is rewritten to scenario-local paths.
- Fixed Scenario-mode Unit Civilopedia icon saves so uploaded large/small PediaIcons art outside the scenario is copied into `Art\Civilopedia\Icons\Units\` and rewritten to scenario-local paths, leaving animation folders and the `units_32.pcx` atlas as separate unit-art concerns.
- Fixed Scenario-mode Civ art saves so uploaded Civilopedia race icons, leaderhead portraits, and advisor portraits are copied into their distinct scenario art folders and rewritten to scenario-local paths.
- Fixed Scenario-mode Resource and Government icon saves so uploaded art outside the scenario is copied into `Art\Civilopedia\Icons\Resources\` or `Art\Civilopedia\Icons\Governments\` instead of the generic icons folder.
- Fixed Scenario-mode Improvement art saves so uploaded building icons and wonder splash files outside the scenario are copied into `Art\Civilopedia\Icons\Buildings\` and PediaIcons is rewritten to scenario-local paths.
- Fixed Improvements category changes so switching an entry to Wonder or Small Wonder immediately shows the Wonder Splash selector in `Building Art`.
- Fixed Improvement PediaIcons round-tripping so era/culture-specific building icon blocks preserve every positional art path, including intentional duplicate paths used by vanilla buildings such as Granary.

* RELEASE 27.5
- Added optional daily debug log files, enabled by default, with grouped File menu controls for toggling logging, choosing/resetting the log folder, and opening the log folder directly.
- Renamed the Units `Capacity` statistic label to `Transport Capacity`.
- Added compact `Remove All` actions to Units list editors for Unit Abilities, Available To, and Stealth Attack Targets.
- Added C3X base manifest coverage for newly shipped default keys, including expanded unit stat display toggles, resource perfume, battle-created-unit spawning, impassable-tile barbarian placement, preplaced scenario leader initialization, and the corrected Great Wall impassable key spelling.
- Fixed the Units rules layout so non-checkbox Special Orders fields render with their proper editors, restoring the `Enslave Results In` unit picker and `Telepad Range` numeric field.
- Fixed localized C3X sectioned config files, including Districts, Wonder Districts, Natural Wonders, and Tile Animations, so they use the text-file encoding detector/override on load and preserve the detected encoding on save instead of forcing UTF-8.
- Fixed Improvement/Small Wonder PediaIcons saves so building icon blocks preserve the `SINGLE` marker, in-city icon index, large Civilopedia icon, small Civilopedia icon, and wonder splash art instead of degrading to generic path-only blocks.
- Changed Civilopedia and PediaIcons text saves to insert new autogenerated sections before terminal markers such as `#EOF` and `#END CIVILOPEDIA ART`.
- Added quality-audit warnings for case-mismatched Civilopedia `$LINK` targets using the raw mixed-case source keys, since Civ3 resolves those links case-sensitively.
- Added red wavy Civilopedia preview highlighting and a per-link `Fix` action for safe case-only `$LINK` target repairs.

* RELEASE 27.4
- Refactored compact boolean styling to use shared CSS tokens across Units, Improvements, Techs, and Civs while preserving the existing per-screen selectors and layout hooks.
- Matched the compact Improvements and Units boolean toggles so inline checkbox rows now use the same smaller font and checkbox sizing as the denser rules layout.
- Added shipped C3X base key `exclude_passengers_from_stealth_attack` to the manifest-backed C3X editor, quality audit coverage, and audited default-config fixture so stock newer defaults no longer warn as unknown.

* RELEASE 27.3
- Added the missing shipped R27 C3X base keys `reformat_turns_remaining_on_domestic_advisor_screen`, `patch_passengers_out_of_order_on_menu`, `can_bombard_only_sea_tiles`, `allow_corruption_in_capital`, `special_capital_decorruption_effect`, `override_no_ai_patrol`, and `override_barbarian_activity_level_for_scenario_maps` to the manifest-backed base editor, quality checks, and audited default fixture.
- Fixed the district/wonder audit validator so `buildable_without_removal = marsh` is recognized as valid instead of incorrectly warning about an unknown list value.

* RELEASE 27.2
- Restyled shared search-field active borders to use the app theme accents with a brighter focus ring and subtle glow instead of the old flat gray focus treatment.
- Tuned the shared search-field rest state back toward the previous low-border look and moved the focus emphasis inward so the active edge no longer gets clipped at container boundaries.
- Balanced the spacing around shared compact collapsible stacks so the gap below sections like `Other Art` matches the gap above `Civilopedia`.
- Normalized compact collapsible section spacing so shared panels like `Civilopedia`, `Other Art`, and `Animation` use the same gap and visual rhythm across tabs.
- Changed `Resources -> Type` to use the same segmented choice UI style as `Improvements -> Category` instead of a dropdown.
- Removed the extra Resources rules wrapper/header so the tab no longer shows a `Rules` card title or the `From BIQ section: GOOD` subtitle above its BIQ field groups.
- Changed the Resources tab so its Civilopedia section now appears in a collapsible panel instead of always being expanded.
- Removed the extra top gap above the `C3X` tab search bar so the base-config editor header aligns with the other editor tabs.
- Reworked the Civs tab into a denser responsive dashboard layout with compact identity/personality/free-tech boards, matrix-style traits and governor controls, a smarter multi-column resize model like Techs and Improvements, the same optional right-hand sidebar toggle, and a collapsible Civilopedia section.
- Reworked the Techs tab into a denser dashboard layout with a compact top board for era/cost/prerequisites/tree controls, matrix-style capability groups, the same optional right-hand sidebar toggle used by Units and Improvements, and a collapsible Civilopedia section while keeping the sidebar visible by default.
- Reworked the Improvements tab into a first-pass dense dashboard layout with grouped columns, more compact top metadata, and the same optional right-hand sidebar toggle used by the Units tab; the sidebar still defaults to visible for Improvements.
- Refined the native search-bar clear `X` hover treatment to use a rounded background highlight instead of filter-only feedback, making the affordance feel more button-like.
- Styled the native search-bar clear `X` affordance with a pointer cursor and hover/press feedback so clearing search feels more obviously clickable.
- Matched shared search-bar typography to the main tab labels so search inputs now use the same family, size, and weight style as tabs like `C3X`, `Techs`, and `Civs`.
- Removed the remaining explanatory `hint` text above the search bars in the `C3X`, `Districts`, `Wonder Districts`, and `Natural Wonders` tabs to keep those editor headers tighter.
- Removed the tab-level header strip above editor search bars, including duplicated `effective`, `editable`, and similar status pills, and pinned those search/action rows directly to the top of each tab instead.
- Deepened the Scenario mode background tint slightly in the upper-right so it better matches the visual strength of the Standard Game mode backdrop.
- Moved `Districts`, `Wonder Districts`, and `Natural Wonders` `+ Add` and `Delete` actions into the same inline search-row control area used by Civs, Techs, Units, and other entity editors.
- Reworked the top header into a single inline title-and-mode-controls row, removed the separate mode status strip, and folded the app version into the lower-left credit line as a compact badge.
- Fixed scenario `PediaIcons.txt` tech icon saves to write Windows-style relative paths like `Art\tech chooser\Icons\...`, and to strip leaked source scenario folder prefixes such as `Eldorado5\` that could crash Civ III during load.
- Fixed scenario unit INI animation saves to rewrite scenario-root or imported `Art\Units\...` references into valid Windows-relative animation paths, preventing leaked scenario prefixes and slash-style breakage in saved unit entries.
- Removed the redundant top-center scenario name chip so loaded scenarios are shown only in the scenario mode selector instead of being duplicated in the header.

* RELEASE 27.1
- Adjusted `C3X -> Unit Limits` row spacing to use the same card-style item layout as `Production Perfume`, making those structured entries visually consistent.
- Removed the hover tooltip from Districts tab entries in the left-hand district list so hovering those rows no longer shows source/file metadata.
- Improved C3X base-tab responsiveness in large scenarios by moving continuous base-field typing onto the existing grouped undo-session path instead of taking a full editable-bundle snapshot on every keystroke; dirty tracking, save output, and discrete undo actions still use the same save/restore pipeline.
- Changed the C3X base tab to construct offscreen row editors lazily as they approach the viewport, reducing initial tab-open cost on large scenario configs without changing validation, save, or undo state tracking.
- Added an Era dropdown next to search in the Techs and Units tabs, populated from the loaded scenario/base-game ERAS data and off by default; Units filter by the era of their required tech.
- Added localized encoding support for Civilopedia/PediaIcons/diplomacy text files and BIQ-backed names/labels, including auto-detection for common Civ3 code pages, same-encoding save preservation, and a global UI override when auto-detect is ambiguous.
- Unit and improvement prerequisite resource dropdowns now include bonus resources, matching Quintillus Editor instead of restricting those BIQ pickers to luxury and strategic resources.
- Reduced a renderer leak that could accumulate global click handlers while reopening Civilopedia editors and picker widgets, which could lead to random white-screen renderer failures during typing/clicking/dragging on Windows.
- Added explicit renderer crash and hang logging plus a recovery message when the Electron content process dies, so white-screen failures are now easier to diagnose.
- Changed Civilopedia editing so live preview stays on while typing, but undo now groups the entire edit session into a single change when you click `Done`, instead of capturing every keystroke as a separate undo step.
- Fixed undo dirty-badge restoration so reverting a Civilopedia or other grouped edit now fully clears the entry/tab modified badges when the restored data matches the clean state.
- Reduced tech tree drag-time rendering overhead by throttling node movement updates to animation frames and caching node metrics for edge routing, which should make dragging smoother and reduce white-screen renderer failures on large trees.
- Reworked other continuous text/number editors, including BIQ rule fields and diplomacy editors, so undo snapshots are grouped by edit session instead of being recorded on every keystroke.

* RELEASE 27.0
- Changed `New -> Create from Base Game` to use a typed `Search Folder` field instead of a browse-only folder path, matching the copy-scenario flow and defaulting that field from the scenario name.
- Fixed base-scenario creation so pre-R28 C3X installs no longer fail when `default.tile_animations.txt` is absent; tile animations are now treated as optional during scenario creation.
- Improved scenario creation/save-as failure feedback so users now get an in-app error status, failure toast, and details summary instead of a silent modal close when creation fails.
- Fixed `C3X -> Production Perfume` so the type dropdown only offers the documented `Improvement` and `Unit` options; unknown existing names are still preserved/editable through the picker.
- Fixed `Building Prereqs for Units` parsing so quoted multi-word unit names like `"Supply Barge"` and `"Dark Supply Barge"` no longer get collapsed into comma-joined tokens in the UI.
- Tightened several structured C3X base editors so `Unit Limits` now uses a denser inline row layout, `Building Prereqs for Units` better visually nests units under their building, and `Buildings Generating Resources` now uses the same friendly labeled flag chips as district generated resources.
- Fixed district preview invalidation when switching game/mod context so the initial district art no longer reuses stale art from the previous config until another picker click.
- Fixed district representative preview initialization so era/building picker highlights now stay in sync with the initially shown district art, including suppressing the transient flash from default picker states.
- Added `render_strategy = by-building` district preview support so the preview composites base art with multiple selected building layers and the Buildings picker becomes multi-select in that mode.
- Added the remaining documented R27 district fields to the structured district editor, including `render_strategy`, `generated_resource`, `resource_prereq_on_tile`, `img_column_count`, `ai_build_strategy`, ally-build flags, impassibility flags, irrigation-source, and one-turn healing.
- Fixed district `generated_resource` and `resource_prereq_on_tile` serialization/parsing parity with C3X, including whitespace-separated generated-resource flags and correct quoting for multi-word resource names.
- Improved district generated-resource editing so the UI now uses a real resource picker plus structured `Local`, `Yields`, and `No Tech Requirement` toggles instead of raw freeform flag text.
- Added broad district config parity coverage and district preview-selection regression tests for district field round-tripping and representative building-button normalization.
- Fixed C3X bitfield-list base settings such as `special_zone_of_control_rules`, `special_defensive_bombard_rules`, `land_transport_rules`, and `special_helicopter_rules` to serialize in C3X-compatible whitespace-delimited bracket form instead of comma-separated lists.
- Added an explicit audited manifest for all 240 shipped C3X base keys, including typed family metadata for enums, bitfield lists, quoted reference lists, quoted strings, and other structured base-field serializers.
- Added stronger C3X base audit coverage so tests now verify full shipped-key manifest coverage plus save/reload syntax parity for every explicit C3X string-family field.
- Fixed C3X quoted unit/improvement reference-list settings such as `ptw_like_artillery_targeting`, `exclude_types_from_units_per_tile_limit`, `limit_defensive_retreat_on_water_to_types`, and `ai_multi_start_extra_palaces` to use C3X-compatible whitespace-delimited quoted list syntax instead of comma-separated lists.
- Fixed additional C3X enum parity issues, including the missing `if-not-slower` and `if-fast-and-not-slower` retreat-rule options and the source order for `double_minimap_size`.
- Added scalar-family C3X base audit coverage so every boolean and integer base key now has explicit save/reload syntax tests, and added an audit-tier completeness test so every manifest key must belong to a named audit bucket.
- Unified segmented chip controls across the app so shared single-choice and multi-choice chip UIs now consistently use the same green active styling.
- Added a scenario tech tree civilization filter with civilization thumbnails so modders can preview civ-specific research trees that depend on hidden free-tech gates.
- Expanded and refined the tech tree modal layout, including larger presentation, tighter hint text, a default `All Techs` filter on fresh open, and no horizontal scrollbar.
- Fixed tech tree undo behavior so `Undo` correctly restores node positions, the restore overlay appears above the tech tree modal, and the toolbar now includes both `Undo` and `Undo All` without clearing the active civ filter during undo refreshes.
- Added an optional civ-filtered non-era lane in the tech tree so faction gate techs with `era = None` can be shown separately from the visible era graph, including Tides of Crimson-style civ-specific free-tech seeds such as `BREHTONS` and `GOOD-NEUTRAL`.
- Refined the tech tree split layout so the visible era graph stays in the right pane, the left pane uses the same title treatment as the era label, and non-era techs are distributed within the visible era's vertical span instead of clumping at the top.
- Fixed multiple tech tree regressions while adding the non-era lane, including proper `None` era classification, safer gutter graph traversal, and explicit lane toggling behavior.
- Tests: pinned `c3xBaseManifest` coverage to a checked-in default base config fixture so manifest audits no longer depend on the local sibling `C3X_Districts` install contents.
- Scenario loads now kick off a background quality-audit worker that surfaces file/asset warnings without delaying the initial render, including proactive day/night terrain and district hour-art validation.
- Added background quality-audit checks for missing on-disk reference art in the Civilizations, Technologies, Resources, Governments, Improvements, and Units tabs.
- C3X tab field search now also matches each setting's release badge, so queries like `r26` find fields introduced in that release without losing normal name/description search.
- Added configurable text-file encoding support for Civilopedia, PediaIcons, and diplomacy assets, including auto-detection for common Civ3 code pages, source-encoding preservation on save, and a global UI override for localized installs/mods.
- Added first-pass non-blocking config lint checks for unknown keys and invalid boolean/integer/select/list values in C3X base config plus Districts, Wonder Districts, and Natural Wonders.
- Fixed several C3X base-field editor mismatches with the actual parser, including proper `limit_railroad_movement` editing, preserving empty bitfield-list selections, and recognizing special limit fields as non-boolean.
- Added a `File -> Settings -> Run Quality Checks After Load` preference, enabled by default, so users can disable the background post-load audit warnings if they prefer.
- Moved `Text File Encoding` out of the top file-path panel and into `File -> Settings` so file-path controls stay focused on location selection.
